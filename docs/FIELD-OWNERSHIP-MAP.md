# Field & engine ownership map

**See also:** [Architecture Freeze — Plan Catalog vs Subscription Contract Isolation](ARCHITECTURE-FREEZE-PLAN-CONTRACT-ISOLATION.md) (LOCKED architectural reference for paid-member contract vs editable catalog).

**Purpose: stop duplicates before they are written, cheaply.**

The frozen no-duplicate rule (one fact = one input = one destination = one
engine) is enforced by *knowing what already exists*, not by running tests. This
file is the lookup that makes that check take seconds instead of minutes.

**Use it like this — before adding ANY new field, column, service, engine,
endpoint, or UI input:**

1. Search this file for the fact you are about to store.
2. If it is listed → bind to the existing owner. Do not add a parallel field.
3. If it is genuinely absent → grep to confirm (`grep -rn "your_field" app/ database/migrations/`),
   add it, then **add a row here in the same commit**.

If this file and the code disagree, the code wins — and this file is stale and
must be fixed.

---

## 1. Canonical owners — one fact, one home

| Fact | Canonical home | Written by | API key(s) | Traps / aliases |
|---|---|---|---|---|
| Cross-Suchak name visibility | `suchak_profile_representations.shares_name` + `shared_display_name` | originating Suchak, per candidate | `display_name` (masked summary) | Default is `CandidateNameMask`. **Do not add a second mask** — `SuchakCandidateMaskingService::displayName()` is the only decision point. `full_name` is ONE column: there is no surname to peel off, which is why the Suchak may type `shared_display_name` instead. |
| Cross-Suchak village visibility | `suchak_profile_representations.shares_village` | originating Suchak, per candidate | `location.city`, `location.is_broad`, **and the location precision of `match_field_points` / `reasons`** | **One reader only: `SuchakCandidateMaskingService::revealsVillage()`.** The card (`locationSlot()`) and the fit explanation (`SuchakMatchFitService::fit()` → `MatchingService::computeMatchBreakdown($…, capLocationAtTaluka:)`) both take their answer from it — do not read the column a second time. **Bit us 2026-08-01:** `city` was never a city — `locationNameForCitySlot()` returns the *village*, while `is_broad` was hardcoded `true` beside it. Default is now taluka. **Bit us again 2026-08-03:** the card masked it and the SCORE beside it confirmed it (trap 14). If you change one, change the flag — and check the score too. **Bit us a third time 2026-08-04:** these two rows are the CROSS-Suchak rule and were being applied to a Suchak's OWN candidate — `SuchakMarketplaceChallengeService::published()` (his "My challenges" tab) built rows with the masked presenter, so the platform printed his own customer back at him as "राजश्री ग." with her village withheld. Fixed by `ownCandidateSummary()` — the OWNER's row answers name / village / exact address / `is_broad` for himself and takes every other fact from the one presenter; `browse()`, `openListing()` and `proposalPayload()` are untouched. **Before masking anything, ask who is reading.** |
| Cross-Suchak address / mobile visibility | `suchak_profile_representations.shares_detailed_address`, `shares_mobile` | originating Suchak, per candidate | `location.exact_address`, `contact.phone` | Both were hard-null before and stay null unless revealed. Member app is NOT covered by any of these — a village is never shown to a member (blueprint D19b). |
| Frozen registration price + per-meeting / success fee (**the five money columns — one quote, one rule**) | `suchak_service_packages.price_amount`, `per_meeting_fee_amount`, `per_meeting_online_fee_amount`, `post_marriage_fee_mode`, `post_marriage_fee_amount` | `SuchakPackageCatalogService::normalizedPlanTerms()` — the single definition, used by materialisation (`createCustomPackage`) and re-quote (`planTermsDrift` + `applyPlanTerms`) alike | inside `agreement_snapshot_hash`; `price_amount` is additionally copied onto `suchak_customer_agreements` at snapshot time | **Bit us 2026-08-01:** the rates live on `suchak_customer_plans` (mutable, un-versioned). They never reached the package or the hash, so "the customer accepted these fees" was false. Rate → package → frozen agreement is three homes for three different questions; do not add a fourth. **Bit us again 2026-08-01 (preset branch), twice:** `/payment-setup` accepted the four fees but applied them only on the CUSTOM branch; and the preset branch pinned `price_amount` to the hardcoded `SuchakDefaultPlans` figure, ignoring both the posted price and the Suchak's own `suchak_customer_plans` override — so a device test showed an acceptance page reading "नोंदणी शुल्क ₹2,000" while the WhatsApp message said otherwise. **Rule now, all five fields, both branches: a figure posted with the send beats the plan's default. The plan is the DEFAULT, the send is the DECISION.** One resolution point only: `presetPlanTerms()` (default layer) overlaid by `submittedPlanTerms()` (decision layer) in `SuchakPaymentSetupApiController`; do not add a third resolver. Asymmetry to keep: a missing FEE is an honest "ठरलेले नाही" and freezes null, a missing PRICE is a broken quote and falls back to the default instead. Re-quoting a package is allowed ONLY while no revision of its agreement is satisfied — once accepted/bypassed/not-required the send is refused (422, Marathi, both figures named), because `PublicAgreementController::termsFor()` reads these columns LIVE and editing them would rewrite a document a family already signed. |
| Suchak business type | `suchak_accounts.business_type`; allow-list is `SuchakAccount::BUSINESS_TYPES` | web register, `/suchak/register/identity` (APK) | `business_type` | Exactly TWO values: `individual`, `bureau` (विवाह मंडळ). **Bit us 2026-08-01:** the allow-list was hand-restated in four validators with no shared constant, `bureau` was a dead constant nothing accepted, and `organization` was the live third value. Renamed `organization` → `bureau`; every validator now reads `BUSINESS_TYPES`. Do not restate the list inline. Labels come from `suchak.business_types.*` / `suchak.labels.common.*` — not from `ucfirst()`. Note `organization_logo` (verification-record type, upload route, `organization_logo_url`) is a DOCUMENT name, not a business type — deliberately unchanged. |
| Suchak team size | `suchak_accounts.employee_count` | web register form, `/suchak/register/identity` | `employee_count` | Required whenever `business_type = bureau`. **Bit us 2026-08-01:** the web form never rendered this input, so web bureau/organization registration was unsubmittable; the APK sends a silent default of `1`. Add the input, do not relax the rule. |
| Candidate gender | `matrimony_profiles.gender_id` | manual-create + `basic_info` snapshot | `candidate_gender` (create, key), `gender_id` (snapshot, id) | **Bit us 2026-07-22:** the Suchak wizard rendered two unlinked gender inputs. One visible input only; the second consumer must receive the value, never re-ask. |
| Mother tongue | `matrimony_profiles.mother_tongue_id` | `basic_info` snapshot | `mother_tongue_id` | **Bit us:** removing the community-step *gate* also removed *collection*. Removing a gate ≠ removing the field. |
| Marital status | `matrimony_profiles.marital_status_id` | full PUT / `basic_info` | `marital_status_id` | Status **keys** are owned by `MaritalDependencyRules` — never hardcode `['divorced','annulled','separated','widowed']` again. |
| Previous marriage detail | `profile_marriages` rows | full PUT only | `marriages[]` | The step-snapshot service **rejects** `marriages` (422). Only the full PUT runs the marital engine. |
| Children | `profile_children` rows + `matrimony_profiles.has_children` | full PUT | `children[]`, `has_children` | `children_count` exists for **member mobile onboarding only**. **Bit us:** the Suchak wizard invented a "number of children" field and wrote blank rows. Web/Suchak edit = repeatable rows (gender, age, `child_living_with_id`). |
| Candidate's own mobile | `profile_contacts` (primary) | manual-create (`candidate_mobile`, required since 2026-07-22) | `candidate_mobile` | Not a column on `matrimony_profiles`. |
| Parent contacts | `matrimony_profiles.father_contact_1/2`, `mother_contact_1/2` | full PUT | same names | `_3` variants only exist on some deployments — guarded. |
| Sibling contacts | `profile_siblings.contact_number`, `_2`, `_3` | full PUT | `siblings.*.contact_number*` | Accepted since 2026-07-21. Owner-only on read; stripped for other viewers. |
| Relative contact | `profile_relatives.contact_number` | full PUT | `relatives.*.contact_number` | `_2`/`_3` are **prohibited** — no columns exist. |
| Contacts that do NOT exist | — | — | — | `marriages.*`, `children.*`, `*_addresses.*`, `alliance_networks.*` contact fields are **deliberately prohibited** (no columns; would silently drop). Do not "add support". |
| Personal income | `matrimony_profiles.income_amount` (+ `income_period`, `income_value_type`, `income_min/max_amount`) | career step / full PUT | `income_amount`, `income_period`, … | `annual_income` is the **derived flat** column; `income_normalized_annual_amount` is what **member search** uses. Derived-from is allowed by the frozen rule — these are NOT duplicates. Verified 2026-07-23. **Corrected 2026-08-03: "search uses the normalized column" is true of the MEMBER corpus only.** The derivation is one-way — `MatrimonyProfileApiController::mobileIncomeEngineCoreFromApi()` writes `annual_income = request('annual_income') ?: $normalized`, and the Suchak app sends the flat figure alone (2026-07-25), so `income_normalized_annual_amount` is **NULL for every Suchak-created candidate**. Anything comparing income across the Suchak corpus must read `IncomeEngineService::comparableAnnualSql()` / `comparableAnnualAmount()` — `COALESCE(normalized, flat)` — which is one resolution rule, not a third column. A normalized-only filter there returns an empty list that reads as "nobody earns that much". |
| Candidate photo | `profile_photos` rows (+ legacy `matrimony_profiles.profile_photo`) | photo upload | `primary_photo_url`, `approved_photo_url`, `photo_approved` | API exposes **approved** URLs only, so a freshly uploaded photo returns no URL — treat "photo exists but no URL" as *pending*, not *none*. |
| Suchak identity | `suchak_accounts.mobile_number` | Suchak registration (OTP) | — | **Bit us 2026-07-23:** the admin list showed `user.email`, which is empty for OTP signups, so every row was just a name. Mobile is the real identity. |
| Suchak org name | `suchak_accounts.office_name` / `office_name_mr` | registration | — | Organization rows are meaningless without it. |
| Suchak signup progress | `suchak_accounts.onboarding_step` + `registration_completed_at` | `SuchakRegistrationService` | — | Steps: `otp → identity → location → complete`. Distinguishes "abandoned signup" from "waiting on admin". |
| Suchak account state | `suchak_accounts.verification_status` (+ `public_status`) | `SuchakAccountLifecycleService` | — | The two co-vary in the normal case — show as **one** signal; call out `public_status` only when it diverges. |
| Bilingual labels | `*_mr` sibling columns | — | — | Use `BilingualMasterLabel`; do not invent a second translation path. |
| A phone that may receive push | `device_tokens` (`DeviceToken`), polymorphic `tokenable`: `User` for member-app devices, `SuchakAccount` for Suchak-app devices | `App\Services\Push\DeviceTokenService` — the ONLY writer | member `POST/DELETE /api/v1/device-tokens`; Suchak `POST/DELETE /api/v1/suchak/device-tokens` | **`token` is globally UNIQUE, not unique-per-owner.** An FCM token identifies a DEVICE; re-registering one RE-POINTS the row at the new owner instead of inserting a second row, because two rows would keep pushing to a phone its previous owner no longer controls — a privacy leak, not just a duplicate. DELETE is scoped to the owner (logout); only FCM's own `UNREGISTERED`/`INVALID_ARGUMENT` triggers the unscoped `forgetDeadToken()`. |
| "Do I want this notification at all?" (the EVENT) | `users.notification_preferences` — ONE flat key per event | `App\Services\UserNotificationPreferencesService` | `GET/PUT /api/v1/notification-preferences` (+ `/suchak/…`); legacy engagement keys also on `GET/PUT /api/v1/settings` | **Nearly bit us 2026-07-27:** the push registry first invented `inactive_reminder` / `new_matches` beside the existing `engagement_inactive_reminder` / `engagement_new_matches_digest`, which would have given one fact two keys and two screens that disagree silently. Resolved by `PushTypeRegistry::preferenceKey()` — an event that already had a key KEEPS it; only events born with push use their registry key. Never add a `push_<type>` shadow key; pinned by `DeviceTokenRegistrationTest::the_new_matches_digest_fact_has_exactly_one_storage_key`. |
| "…but not on this channel" (the CHANNEL) | `users.notification_preferences['channels'][<event key>][<channel>]` | `UserNotificationPreferencesService::saveChannelOverrides()` | `PUT /api/v1/notification-preferences` `categories` map | **A different axis from the event above — do not collapse them.** Lets a member keep the new-matches digest by email while silencing it on their phone. Absent = follow the event switch. Adding WhatsApp/SMS later adds entries inside this map, never a key per event-channel pair. `saveForUser()` merges into the raw stored blob for this reason — rebuilding from `forUser()` (flat booleans only) silently wiped this map, which the test suite caught. |
| A Suchak's notification preferences | the SAME `users.notification_preferences` row, reached via `suchak_accounts.user_id` | `UserNotificationPreferencesService::resolveUser()` | `GET/PUT /api/v1/suchak/notification-preferences` | `suchak_accounts.user_id` is NOT NULL and UNIQUE, so every Suchak has exactly one User — **one preference engine serves both actors and there is no Suchak-side copy.** A person who is both member and Suchak has ONE preference set (one human, one choice about being interrupted); the two apps still show different category lists via `PushTypeRegistry::forApp()`. Device TOKENS stay owned by the SuchakAccount, so the two app installs remain addressable separately. |
| Which notification types may push, platform-wide | `admin_settings` rows `notification_push_type_<push key>` (+ `notification_push_enabled`, `notification_push_quiet_hours_*`) | `NotificationPlatformSettingsService` | Admin → App settings → Notifications | Runtime switchboard so the product owner can turn a type on/off with no code change and no new APK. Absent row falls back to the registry's `default_push`, so a newly added type is never an error. **Admin OFF beats user ON**, and an admin-disabled type is hidden from both apps' settings screens entirely — showing a switch the member's choice cannot affect is worse than hiding it. |
| Per-Suchak reusable customer plan | `suchak_customer_plans` (`SuchakCustomerPlan`) | Suchak via `SuchakCustomerPlanService` | mgmt `GET/POST/PUT/DELETE /suchak/customer-plans`; carousel via `resolveCarousel()` | **Materialized into `suchak_service_packages` at SEND** through `SuchakPackageCatalogService::createCustomPackage` — **never FK-linked** back. **ONE kind of row since 2026-08-02:** `preset_key` NULL = a plan the Suchak built, `basic`/`premium` = a ready-made plan **seeded** from `SuchakDefaultPlans` and thereafter editable in full (it is no longer a five-field "override" — that is what made the plan uneditable). ONE writer `applyPlanFields()` and ONE reader `planEntry()` serve both kinds; a `preset_key` row is **undeletable** (hide it) because that key scopes the customer's package at send time. `PUT /suchak/customer-plans/{basic|premium}` addresses a ready-made row by key (what the shipped app knows) and seeds it first if missing. NOT the platform `suchak_plans` catalog. `private_note` is Suchak-only, never in the customer carousel. |
| Candidate horoscope facts (rashi, nakshatra, charan, gan, nadi, yoni, devak, kul, gotra, navras, birth weekday) | `profile_horoscope_data` (one row per profile, `ProfileHoroscopeData`) | horoscope wizard step / full PUT / `MutationService` | `horoscope.*` | The row EXISTING is not the same as the row being FILLED — every column is nullable, so a row of NULLs is normal. Never read "row exists" as "data present"; that was defect 2 below. `varna_id` / `vashya_id` / `rashi_lord_id` columns exist here but are **derived from `rashi_id`** and must not be collected as separate inputs — `HoroscopeRuleService` derives them. |
| Manglik status (मंगळ) | `profile_horoscope_data.mangal_dosh_type_id` → `master_mangal_dosh_types` | manual entry only | `horoscope.mangal_dosh_type_id` | **The only home for this fact.** It is NOT derivable from rashi/nakshatra and is NOT part of the 36 gunas — Ashta-Koota does not contain Mangal. Compare it only through `MangalCompatibility`. 7 lookup values; `don_t_know` / `other` mean UNKNOWN, never "no". |
| Yoni (योनी) vocabulary | `master_yonis`, **canonical Sanskrit keys only** (`ashwa`, `gaja`, `mesha`, `sarpa`, `shwan`, `marjar`, `mushak`, `gau`, `mahish`, `vyaghra`, `mrga`, `vanar`, `nakul`, `singh`) + the `other` sentinel | `MasterLookupSeeder::seedYonis()`; derivation in `NakshatraAttributesSeeder` | `yoni_id` | **Bit us (fixed 2026-07-26):** an English duplicate set (`horse`, `elephant`, …) sat beside the Sanskrit rows, both active. See trap 10. Sanskrit is canonical because `master_nakshatra_attributes` derives from it. Retired spellings still resolve via `GunamilanMasterData::YONI_ALIASES`. Never add male/female polarity rows (`YoniPolaritySeeder` is a retired no-op). |
| Nakshatra → gan / nadi / yoni derivation | `master_nakshatra_attributes` | `NakshatraAttributesSeeder` | — | The autofill authority. A blank `gan_id`/`nadi_id`/`yoni_id` on a profile is filled from here at scoring time, so a blank column is never a scoring penalty. |
| Rashi → varna / vashya / rashi lord derivation | `master_rashis.varna_id` / `vashya_id` / `rashi_lord_id` | `AshtakootaMasterSeeder` | — | Varna, Vashya and Graha Maitri have **no user input** — they are pure functions of rashi. Do not add dropdowns for them. |
| "Only show me matches whose गुणमिलन works out" | `profile_preference_criteria.gunamilan_required` (boolean, default false) | web horoscope wizard section; member app + Suchak app kundali section via `MatrimonyProfileApiController` (2026-07-26) | `gunamilan_required` — flat, on the SAME profile read/write both apps already use: member `GET/PUT /api/v1/matrimony-profile`, Suchak `GET/PUT /api/v1/suchak/nxt/{representation}/profile` (thin adapter over the same controller). Also mirrored inside `partner_preferences`. | **Asked on the kundali/horoscope screen, stored as a partner criterion** — it is a statement about the PARTNER, but it is collected where the user is already looking at their own patrika. One fact, one input, one destination: there is no second column and no second question anywhere. Terminology is **गुणमिलन / gunamilan**, never "kundali". Only written when the request actually carries the key, so a screen that omits it (every screen except kundali) can never silently clear a saved `true`. Read side always returns a definite boolean, never null, so a brand-new profile still has a toggle state. Crosses three silent allow-lists — `buildMobileProfileSnapshotFromApi()`'s `preferences` fragment, `MutationService::syncPreferencesFromSnapshot()`'s `$allowed`, and `buildGovernanceParityProfilePayload()` — pinned by `tests/Feature/Matching/GunamilanRequiredApiToggleTest.php` (API) and `GunamilanRequiredToggleTest.php` (web). A not-computable gunamilan is UNKNOWN and must not reject a candidate. |
| Taluka / district position ("where is this place?") | `addresses.lat` + `addresses.lng` **on the taluka/district row itself** | `GeoCentroidBackfillService` (migration 2026_07_26_170100 once, then `php artisan locations:backfill-geo-centroids [--force] [--state=]`) | none (internal) | **DERIVED, not surveyed:** the marginal MEDIAN of the row's descendant village coordinates. There is no second home — no centroid table, no cache key, no parallel column; the request path reads this column and never re-aggregates (that aggregate was 73% of a production suggestions request). **Village lat/lng are NOT authoritative** — `addresses` was geocoded BY NAME, so 77.2% of MH villages carry another village's point; never use a village coordinate directly for a nearby decision. Partly repaired 2026-07-26 from India Post — see the village-position row below and read `addresses.geo_source` before trusting any village point. Hence `GeoCentroidBackfillService::accept()`: in-bounds + >=70% of villages within 25 km of the median + <=100 km from the district centre. Measured Maharashtra after the 2026-07-26 village repair: 35/35 districts and **328/358 talukas** accepted, 30 kept NULL (27 low-consensus, 6 far-from-district — a taluka can fail both; e.g. Gaganbawada's median lands 635 km away in Akola). Scope is Maharashtra only, resolved via `addresses.slug`; every other state is NULL by design. **NULL is a valid, safe answer** — `NearbyGeographyResolver::nearbyTalukaIds()` then returns `[$talukaId]` (the anchor itself), which narrows the pool but never mis-points it. **Provenance is the SAME `addresses.geo_source` column the village rows use — no `is_manual` flag, no override table.** Two values at this level: `village_median` (derived here, on district rows as well as taluka rows) and `owner_manual` (a human supplied the centre). Those last 30 were filled by hand by the product owner on 2026-07-26, verified row-by-row against name + parent district, the state bounding box, and <=150 km from their own district centre, then stamped `owner_manual`. **A row stamped `owner_manual` is skipped by the backfill before the `--force` branch is even reached** — it was never derived from the villages, so re-deriving it would restore the exact median the gate refused, and `--force` on a refused row CLEARS the coordinate. `AuditGeoCentroidsCommand` excludes those rows from its drift count for the same reason (their distance from the village median is the point, not a defect). Verify with `php artisan locations:audit-geo-centroids --state=maharashtra` (read-only, exits 1 on a bad stored centre); thresholds live in the service so writer and auditor agree. Regression cover: `tests/Feature/Location/GeoCentroidManualCentreTest.php`. |
| Village position ("where is this village?") | `addresses.lat` + `addresses.lng` **on the village row**, with `addresses.geo_source` naming the provenance of that exact pair | `RepairVillageCoordinatesCommand` (`php artisan geo:repair-village-coordinates [--apply] [--force] [--rollback=<batch>] [--osm] [--state=]`), sourced from the India Post office directory CSV via `App\Support\Location\PostalDirectory` | none (internal) | **The only home for a village's position — no parallel column, no side table of "better" coordinates.** Original values were NAME-geocoded, so same-named villages swapped points across India. Repaired Maharashtra-only on 2026-07-26: of 44,853 MH villages, **6,006 `india_post_name_pincode`** (normalised office name AND pincode match — a real point for that village), **5,638 `india_post_pincode_area`** (pincode only — the marginal median of that pincode's offices, so every village in the pincode shares one COARSE point; residual p50 4.8 / p90 15.2 km), **33,209 `legacy_name_geocode`** (assessed and deliberately left alone). `geo_source` NULL = never assessed (every non-Maharashtra village). **Read `geo_source` before trusting a village point** — a `pincode_area` row is an area, not a place. Five gates, each one added because it caught a measured failure: pincode-district cross-check (933 rows excluded), pincode scatter <=25 km (11,392), office point not bulk-filled across pincodes (570 — the CSV gave 35 Nashik offices one identical point), office agrees with its own pincode peers (579 — pincode 442606 plots an office in Punjab), and never-make-it-worse (18,402 already inside their pincode area + 1,317 already inside their taluka's accepted consensus radius). Against OpenStreetMap on a 27-village panel the error went median 6.1→0.8 km, p90 24.1→11.0 km, max 635→26 km, with no item worse by more than 0.8 km. |
| How a village coordinate was decided, and what it was before | `address_geo_repairs` (one row per assessed village per run: `old_lat`/`old_lng`/`old_geo_source`, `new_lat`/`new_lng`, `decision`, `match_type`, `reason`, `moved_km`, `batch`) | `RepairVillageCoordinatesCommand` | none (internal) | **History, not current state — not a duplicate of `addresses.geo_source`.** `geo_source` answers "what is this coordinate now?"; this table answers "what was it before, and why did it change?". It is also the BACKUP that makes the repair reversible: `--rollback=<batch>` restores `old_lat`/`old_lng`/`old_geo_source` for every row of that batch and deletes the batch. Proven on 2026-07-26: apply → rollback → dry-run reproduced the pre-repair decision counts exactly. Re-running `--apply` is a no-op (rows already carrying a `geo_source` are skipped), so the command is idempotent; `--force` restores each row to its FIRST journalled coordinate before re-deciding. |
| "This candidate was SHOWN to a Suchak for this seeker" + what the Suchak decided | `suchak_match_suggestions` (`SuchakMatchSuggestion`) | `SuchakMatchSuggestionLogService` | none yet — recording only, no route/controller in this phase | **Append-only impression+decision log.** NOT `profile_matches` (that is a replace-on-write cache of *current* top matches — `MatchingService::replacePersistedMatches()` deletes every row for a profile then re-inserts, so it has no history and no decision). NOT `user_match_behaviors` (member-actor actions, no Suchak actor, no impression record), NOT `profile_match_tab_skips` / `interests` / `shortlists` (single outcome signals, nothing about what was offered). Idempotency key `(seeker_profile_id, candidate_profile_id, run_key)`; a later `run_key` deliberately creates a NEW row — that is how a candidate re-surfaces after the ~30-day cooling period. Rows are never deleted or bulk-replaced; a decision only updates its own row's `decision` / `rejection_reason_code` / `rejection_note` / `decided_at`. `score` + `reasons_json` are a FROZEN suggestion-time snapshot — never recompute them in place. |
| A member's account password | `users.password` | `App\Services\Account\MemberPasswordService::set()` — the ONLY hasher on the member surface (`POST /api/v1/account/password` for a change, `PATCH /api/v1/account/details` for the first one set during onboarding, which delegates here) | `password` + `password_confirmation` on both routes | **The rule set is `Rules\Password::defaults()` everywhere** — registration, the emailed reset, the Suchak set-password route and this one. Never hand-write `min:8`. `POST /account/password` deliberately does NOT take `current_password` (a mobile-only member cannot use the emailed link), so it MUST keep its two compensating controls: revoke every other credential, and notify. Writing `users.password` anywhere else re-opens the gap where the password changes but old sessions and reset links survive. |
| A member's own mobile number + its verified stamp | `users.mobile`, `users.mobile_verified_at` | `App\Services\Api\MobileOtpService` — the ONLY OTP issuer/checker on the member surface. Login/registration goes through `sendChallenge()` + `verifyChallenge()`; a signed-in member changing or adding their own number goes through `sendPossessionChallenge()` + `verifyPossession()` (`POST /api/v1/account/mobile-otp/send` + `/verify`, auth:sanctum, `AccountMobileVerificationController`) | `mobile` on send; `challenge_id` + `otp` on verify | **There is no bare write of `users.mobile` on the member surface.** The possession path shares one challenge writer (`issueChallenge()`) and one OTP checker (`consumeChallengeOtp()`) with login — added 2026-08-07 by extracting them, NOT by copying. It differs only in that it writes no `UserConsent` (already accepted), issues no token, creates no user, and refuses a number held by another account with 409 (`users.mobile` is uniquely indexed). OTP always goes to the NEW number; a failed/expired challenge changes nothing. Normalisation is `App\Support\MobileNumber::normalize()` — never hand-rolled. Admin's `Admin\DuplicatePhoneController::updateMobile` is a separate admin-only direct write and is NOT a member path. |
| The engagement / assist object (one customer, two Suchaks) | `suchak_collaboration_requests` + `suchak_commission_agreements`, joined 1:1 by `unique(collaboration_request_id)` | `SuchakCollaborationService` | existing collaboration routes | **There is NO `suchak_engagements` table and there must never be one** (blueprint 6.1, corrected 2026-08-01 before any code was written). The pair already carries two account ids, both candidate profiles, both representations, a status lifecycle, an expiry, the declared split and a separate acceptance per side. Everything the marketplace added is additive columns on these two tables plus one child table — see the three rows below. |
| Which side of an engagement OWNS the customer, and which is HELPING | `suchak_collaboration_requests.customer_owner_side` (`SuchakCollaborationRequest::SIDE_REQUESTING` / `SIDE_TARGET`) | `SuchakCollaborationService::bindCustomerAgreement()` — the single writer, reached from `linkCustomerAgreement()` (direct path) and from `createRequest()` when a challenge is supplied (marketplace path, 2026-08-03) | `POST /api/v1/suchak/collaborations/{collaboration}/customer-agreement` → `customer_owner_side`; `POST /api/v1/suchak/marketplace/challenges/{challenge}/proposals` → the same pair, written at creation | **A side LABEL, never a second copy of an account id** — read it through `customerOwnerSuchakAccountId()` / `helpingSuchakAccountId()`, which resolve it against the existing `requesting_`/`target_suchak_account_id` columns. It exists because the pair is stored by DIRECTION and in the marketplace the responder is the requester, so direction no longer implies role. Derived, never typed: linking the customer agreement sets it from whose agreement it is. Default `target` matches the legacy meaning of `suchak_commission_agreements.collector_suchak_account_id` (the target side collects), which stays the money-collection lock and is **not** the role label. **The same one switch also resolves the two PROFILE columns (2026-08-03):** `matrimonyProfileIdForSide()` is the primitive, and `customerOwnerMatrimonyProfileId()` / `helpingMatrimonyProfileId()` are its role readers, exactly mirroring the account pair above. Read "which candidate is the customer's own / which is being proposed" through them and nowhere else — `SuchakTwelveMonthClauseService::proposedCandidate()` and `CustomerStageDoorController::engagementRows()` both used to guess it positionally ("not the requesting one, unless it is"), which silently returned the REQUESTING profile whenever the customer's candidate was on neither side. |
| The customer agreement REVISION an engagement was formed under | `suchak_commission_agreements.customer_agreement_id` → `suchak_customer_agreements.id` | `SuchakCollaborationService::bindCustomerAgreement()` (write-once), from `linkCustomerAgreement()` or from `createRequest()` with a challenge | `POST /api/v1/suchak/collaborations/{collaboration}/customer-agreement` (body `customer_agreement_id`); `POST /api/v1/suchak/marketplace/challenges/{challenge}/proposals` (frozen from the challenge, not supplied) | A revision **is a row** (`agreement_revision` + `supersedes_agreement_id`, unique per `service_package_id`), so the revision is an id — do not add a revision number beside this FK and do not build a revisions table. It sits on the commission agreement because that is the row declaring the share, and the share is a share *of* these terms; that is what keeps it claimable a year later. Re-binding a different agreement throws. **On a marketplace proposal it is written at CREATION, from `suchak_marketplace_challenges.customer_agreement_id`** — the challenge already froze the publisher's accepted revision and only he could have published it, so the role is a recorded fact from row one rather than the `customer_owner_side` default. That also closes a hole the write-once rule then guards: the HELPER cannot afterwards call `linkCustomerAgreement()` with an agreement of his own and appoint himself the customer-owning side. |
| That an engagement is actually ABOUT the customer whose agreement is bound to it | Derived, no column: `SuchakCollaborationService::assertCustomerCandidateSitsOnSide()` compares `suchak_customer_agreements.customer_context_id` → `suchak_customer_contexts.candidate_matrimony_profile_id` against `SuchakCollaborationRequest::matrimonyProfileIdForSide(customer_owner_side)` | Not written — asserted at all three entrances to `bindCustomerAgreement()` / the customer door: `linkCustomerAgreement()` (under the row lock, against the side about to be written), `createRequest()` with a challenge, and `assertPortalLinkGovernsEngagement()` | `POST /api/v1/suchak/collaborations/{collaboration}/customer-agreement`; `POST /api/v1/suchak/marketplace/challenges/{challenge}/proposals`; `POST /suchak/customer-portal/{token}/stages/{collaboration}` | **Added 2026-08-03, the third question the customer door never asked.** Ownership (`isCustomerOwner()`) and context (`customer_context_id` matching the portal link) were both checked and both PASSED on a proven attack: an owning Suchak bound his own agreement to an engagement about two people neither of whom was his customer's candidate, the family's own portal link recorded `viewed`, and `SuchakTwelveMonthClauseService::bindingsForCustomer()` returned a live D11 binding naming a stranger as owed-to for twelve months — the clause working against the family it exists to protect, on the largest sum in the system. A context with a NULL candidate is refused too: "this customer has no candidate on file" cannot establish they are on this pair, and a clause may not bind on an unanswered question. Do not re-derive this question at a fourth call site, and never infer "the other family" positionally — that guess is what let the mismatch through. |
| Which CHALLENGE an engagement answers | `suchak_collaboration_requests.marketplace_challenge_id` (nullable FK → `suchak_marketplace_challenges`) | `SuchakCollaborationService::createRequest()` when a challenge is supplied — the only writer, reached only through `SuchakMarketplaceChallengeService::proposeCandidate()` | `POST /api/v1/suchak/marketplace/challenges/{challenge}/proposals`; read back on `GET /api/v1/suchak/collaborations` (`marketplace_challenge_id`) and `GET .../challenges/{challenge}/proposals` | **NULL means "direct collaboration", and the direct path is untouched by every rule below.** Added 2026-08-03 with its writer, exactly as `2026_08_02_200000` said it must be. Three facts need it and none is derivable: the FROZEN SHARE (D4 — which declaration a live engagement was formed under is a fact about the past, and a republished challenge at a new rate must not re-price it, A8); FULFILMENT (`SuchakMarketplaceChallenge::STATUS_FULFILLED` had no writer until acceptance could find the challenge it closes); and the D4 GUARD (`updateCommissionTerms()` is requester-only and the marketplace requester is the HELPER — the one party who may never move the split, so it is refused outright on a marketplace engagement). `unique(marketplace_challenge_id, requesting_representation_id)` is A2/A10 as a database guarantee — the same candidate cannot be proposed to the same challenge twice, in ANY status, because re-asking after a rejection is not a retry. The pair contains a nullable column on purpose: MySQL and SQLite both treat a tuple containing NULL as distinct, so every non-marketplace row is exempt without a partial index. **Do not add a reverse `accepted_collaboration_request_id` on the challenge** — it is `where marketplace_challenge_id = ? and status = accepted`. |
| The note a Suchak writes when he asks for an engagement — including a helper's note when he answers a challenge | `suchak_collaboration_requests.message` | `SuchakCollaborationService::createRequest()` — the only writer, on BOTH the direct path and the marketplace path | `POST /api/v1/suchak/collaborations` (`message`), `POST /api/v1/suchak/marketplace/challenges/{challenge}/proposals` (`message`); read back on `GET /api/v1/suchak/collaborations` and `GET .../challenges/{challenge}/proposals` (`message`) | **NOT `suchak_collaboration_stage_events.event_note`.** The first cut of accept-by-proposing wrote the helper's one sentence into both — `createRequest()` put it in `message` and `proposeCandidate()` passed the same string to `claimStage()` as `$note` — and both columns were proven to hold the same text. One fact, one input, one destination: the note belongs to the ASK, which exists on the direct path too and has carried this column since day 12, so a marketplace-only second home would also mean two places to read the same sentence from. `event_note` is the note ABOUT one rung, not about the request, and it is not even a durable copy — `confirmStage()` overwrites it with the confirming party's note, so anything duplicated there silently disappears the moment the stage is confirmed. Fixed 2026-08-03; `claimStage()` is now called with a null note from `proposeCandidate()`. |
| Where an engagement stands on the marketplace ladder | `suchak_collaboration_requests.marketplace_stage` (current position) + `suchak_collaboration_stage_events` (one row per stage: claim, confirmation, actors, note) | `SuchakCollaborationService::claimStage()` / `claimCustomerStage()` / `confirmStage()` — the only writers | `POST /api/v1/suchak/collaborations/{collaboration}/stages` (Suchak claims an engagement stage), `POST /api/v1/suchak/customer-agreements/{agreement}/stages` (Suchak claims a pre-engagement stage), `POST /api/v1/suchak-engagements/{collaboration}/stages/confirm` (the CUSTOMER confirms a terminal stage — member API, see below) | **On top of `status`, never instead of it**: `status` stays the pending/accepted/rejected/expired lifecycle; `marketplace_stage` is the blueprint-6a ladder position and moves **forward only**. The vocabulary is `SuchakCollaborationStageEvent::STAGE_LADDER` (ordered) — free text is forbidden because installment triggers hang off these keys, and `stageIndex()` throws on an unknown key. **Every other split is a POSITION on that one ladder, derived by index — never a second hand-written list of stages** (`FIRST_ENGAGEMENT_STAGE`, `FIRST_STAGE_REQUIRING_ACCEPTED_ENGAGEMENT`, and the `preEngagementStages()` / `engagementStages()` / `requiresAcceptedEngagement()` readers over them). **Acceptance is NOT required for `profile_suggested` / `viewed` / `interested`** — a marketplace proposal is created `pending` and D11 attaches the 12-month clause at `viewed`, so gating those on acceptance made the clause's own trigger unreachable (fixed 2026-08-02). The gate starts at `meeting_scheduled`, because arranging a meeting needs contact exchange and `canExchangeContact()` already requires an accepted engagement; before that the engagement must merely be OPEN. `unique(collaboration_request_id, stage_key)` means an engagement stage is reached once per engagement, so a tranche stage can never be claimed twice; each helper's proposal is its OWN engagement, so `viewed`/`interested` are not capped across a challenge's many proposals. Repeat meetings with the same pair are recorded on `suchak_visit_confirmations.meeting_sequence`, not by a second ladder row. The last three stages (`marriage_settled` → `engagement` → `marriage`, `CONFIRMABLE_STAGES`) are claimed then confirmed (D26) and **either Suchak may raise the claim**; the claim alone does not advance the ladder. The 7-day silent-then-dispute timer is Phase 3 and is deliberately absent. |
| Which object a marketplace stage event hangs off | `suchak_collaboration_stage_events.collaboration_request_id` (the engagement) **or** `.customer_agreement_id` (the customer agreement revision) — both nullable, **exactly one set**, both carrying a real foreign key | `SuchakCollaborationService::writeStageEvent()`, reached through `claimStage()` (engagement) and `claimCustomerStage()` (pre-engagement) | `owner` on every stage payload — the literal column name | **Four ladder stages are PRE-ENGAGEMENT facts and had no owner at all until 2026-08-02**: `registration`, `agreement_proposed`, `agreement_accepted` and `published_to_marketplace`. The sharp one is publication — it is the act that INVITES a counterparty, so by definition no collaboration request exists yet, and with `collaboration_request_id` NOT NULL the row could not be written even in principle. The pre-engagement owner is the **customer agreement revision** because blueprint §4 already decided it: *"Publication attaches to whichever agreement is accepted at that moment"* — the challenge declares a share OF those terms (D4), a rate change is a NEW agreement row and never an edit, and A8 needs the declared share to stick to what was published under it. Which owner a stage may use is **derived from ladder position** (`ownerColumnFor()`), and the exactly-one rule is enforced in `SuchakCollaborationStageEvent::assertOwnership()` on `saving` — MySQL and SQLite cannot be given the same CHECK through Laravel's schema builder, so the invariant lives in one model hook every writer passes through rather than twice in two dialects. **Do not replace this with a polymorphic (`owner_type`, `owner_id`) pair**: it cannot carry either foreign key, and "a stage event never belongs to nothing" would stop being a database guarantee. The **challenge object (Phase 2) becomes a third owner** at the cost of its column + FK in its own migration and one entry in `OWNER_COLUMNS` — the guard, both writers and the unique indexes extend unchanged. Grain: `published_to_marketplace` is recordable **once per agreement revision** (`unique(customer_agreement_id, stage_key)`); a re-publish at a new rate is a new agreement row and therefore a new stage row, but a withdraw-and-republish at the SAME rate is not counted here — that count belongs to the challenge object (A12). |
| **The channel a family-owned ladder rung was recorded through** (`viewed`, `interested`, `meeting_confirmed`) | `suchak_collaboration_stage_events.claimed_via_customer_portal_link_id` (nullable FK → `suchak_customer_portal_links`) | `SuchakCollaborationService::writeStageEvent()`, reached only through `recordCustomerStage()` | `POST /suchak/customer-portal/{token}/stages/{collaboration}` (public, tokenised — `CustomerStageDoorController`), listed on `GET /suchak/customer-portal/{token}/stages` | **Added 2026-08-03 because the three CUSTOMER-claimed rungs had no writer at all.** `STAGE_CLAIMANTS` names the customer, `assertStageClaimant()` refused every Suchak (correctly — 9a A2/A3), and there was no customer door, so **zero rows could exist** and D11's 12-month clause had no anchor: `claimed_at` was declared, indexed and unwritable. A customer claim carries **neither** existing claimer column — `claimed_by_suchak_account_id` is null (no Suchak acted) and `claimed_by_user_id` is null (the customer is the FAMILY and usually has no login, §2) — so without this column the row would be one written by nobody. **Bound to the existing customer portal link, NOT a fifth token shape**: of the four tokenised customer links, the agreement-acceptance token and the consent token are SINGLE USE and die on the first decision, and a payment-request token is a money artifact; `suchak_customer_portal_links` is the only one that is re-openable (`opened_at`), records who in the family holds it (`claimed_name`, `claimed_relationship_to_candidate`) and is revocable, and it is already issued in production beside every payment request. **What it proves (D23, §8): that somebody holding that link acted, at that time. NOT who they were** — OTP does not exist on production (§10 S4), so no `mobile_match`, no `*_verified` and no acceptance tier is written on this path; §8 names `recordPublicConsentDecision()`'s unchecked `mobile_match => true` as the one fiction already in this codebase and it is deliberately not repeated. The IP and user agent of the act live on `suchak_activity_logs` (`ACTION_COLLABORATION_STAGE_CUSTOMER_RECORDED`, `actor_type = user`, `actor_user_id = NULL`) and the link's own timeline on `suchak_customer_portal_events` — **neither is copied here**. The exactly-one-channel invariant (a customer rung MUST name a link and MUST NOT name a Suchak claimer; a Suchak rung must do the reverse) is `SuchakCollaborationStageEvent::assertClaimChannel()` on `saving`, beside `assertOwnership()` and for the same portability reason. |
| **The channel a family's CONFIRMATION of a terminal rung came through** (`marriage_settled`, `engagement`, `marriage`) | `suchak_collaboration_stage_events.confirmed_via_customer_portal_link_id` (nullable FK → `suchak_customer_portal_links`) | `SuchakCollaborationService::confirmStage()` — the same single settle path the member and admin doors use, now reachable with a portal link as the actor | `POST /suchak/customer-portal/{token}/stages/{collaboration}/confirm` (public, tokenised — `CustomerStageDoorController::confirm()`), shown on `GET /suchak/customer-portal/{token}/stages` | **Added 2026-08-04 because D26's confirmer could not reach any door.** For the three `CONFIRMABLE_STAGES` `isSettled()` means `confirmed_at`, and `SuchakSuccessFeeTrancheService` releases on settled rungs — so this is the act that turns a Suchak's claim into money. The confirmation half of the table had exactly one identity column, `confirmed_by_user_id`, a `users` FK; §2 says the customer is the FAMILY and usually has no login, so the party D26 names was the one party that column could not hold. The whole success fee therefore released only when an ADMIN stood in (`SuchakAdminTerminalStageApiController`) or when a family member happened to hold a member login AND to be one of the two candidates (`MemberSuchakStageApiController`). **The same argument, and the same answer, as `claimed_via_customer_portal_link_id` one row above**: name the CHANNEL rather than invent an identity, and bind to the existing portal link rather than mint a token shape. **NOT a second settle path** — `confirmStage()` takes `User|SuchakCustomerPortalLink` and the UPDATE, the already-confirmed refusal, the §6.2 marriage-attribution refusal and the ladder advance all stay in the one method; §12 forbids a second confirmation engine. **What it proves (D23, §10 S4): that somebody holding that link confirmed, at that time. NOT who they were** — no OTP, so no `*_match` / `*_verified` / acceptance tier is written, and the page says so to the family in their own language (`suchak.customer_portal.stages.link_proof_note`, reused rather than reworded). IP and user agent live on `suchak_activity_logs` (`ACTION_COLLABORATION_STAGE_CUSTOMER_CONFIRMED`, `actor_type = user`, `actor_user_id = NULL` — its own action, because "the family recorded a rung of their own" and "the family agreed with a Suchak's claim" are the two questions a dispute asks). The exactly-one-channel invariant (a confirmation names a user id **or** a link, never both and never neither; a link-borne one is `ACTOR_USER` and a Suchak is refused outright) is `SuchakCollaborationStageEvent::assertConfirmChannel()` on `saving` — which is why `confirmStage()` now writes through the model instead of the query builder, since a query-builder update fires no model events. **A Suchak is refused at the door too** (`CustomerStageDoorController::actorIsSuchak()`): D26 exists because the party who benefits must not be the party who attests, and both Suchaks benefit. |
| **Whether a family already knew the proposed family** — 9a A6's one-tap release of the 12-month clause | `suchak_collaboration_stage_events.prior_acquaintance_declared` (boolean, default false, meaningful **only** on the `viewed` rung) | `SuchakCollaborationService::writeStageEvent()`, reached only through `recordCustomerStage()` and written in the **same insert** as the `viewed` row it releases | `POST /suchak/customer-portal/{token}/stages/{collaboration}` with `prior_acquaintance=1` (public, tokenised — the family's own checkbox on `GET /suchak/customer-portal/{token}/stages`) | **Added 2026-08-03.** Genuinely new by §5.5's test: nothing in the schema records that two families were already acquainted, and it is not derivable from a profile, a village, a pipeline or a suggestion — only the family knows, and A6 says they say so with one tap. It sits on the `viewed` row because that row **is** the binding it releases (`CLAUSE_ANCHOR_STAGE`, unique per engagement + stage, and the engagement names exactly the one candidate pair) — binding and release in two places is two places that can disagree. **Refused on every other rung** by `SuchakCollaborationStageEvent::assertPriorAcquaintanceRelease()` on `saving`: a release with no binding to release is a value no rule reads, and a column that can hold a meaningless value eventually holds one. **Write-once with the view, never afterwards** — A6 says "at view time", and a release applicable later would be a way to un-owe a success fee once the marriage was already in sight. WHO may set it is **not** restated: `viewed` is a CUSTOMER rung (`STAGE_CLAIMANTS`), so `assertClaimChannel()` has already refused every Suchak on that row — a Suchak able to tick this would be deleting his own obligation, one able to untick it would be manufacturing one. The act is also stamped into `suchak_activity_logs` metadata (`prior_acquaintance_declared`). |
| **That a candidate MARRIED, and which engagement is credited with it** (blueprint §6.2) | `suchak_marriage_outcomes` (`SuchakMarriageOutcome`) — one row per marriage, naming the engagement (`collaboration_request_id`), the agreement revision in force (`customer_agreement_id`, **NOT NULL**), the ladder rung that evidenced it (`stage_event_id` → the `marriage` row), both candidates and `married_on` | `SuchakMarriageOutcomeService::record()` — the only writer | `POST /api/v1/suchak/collaborations/{collaboration}/marriage` (with `married_on`), `GET` the same URI for the attribution card | **Before 2026-08-04 NOTHING recorded a marriage this platform produced, and four things looked as if they did.** `SuchakPipeline::STATUS_CONVERTED` is declared and has **zero writers**. `matrimony_profiles.lifecycle_state = archived_due_to_marriage` is a **visibility** state from a generic admin dropdown — no date, no counterparty, no engagement, no terms — and stays that way. `profile_marriages` is the candidate's **previous** marriage at intake (year granularity + divorce history) and must never hold a platform outcome beside it. `homepage_success_stories` is CMS copy with free-text names. The `stage_key = marriage` ladder row is a **claim on an engagement, not an outcome on a person**. Uniqueness is the whole point (§6.2: "the largest sum in the system has no owner"): UNIQUE on the engagement, on the evidencing rung, **and on each candidate column**, plus a cross-column check under a row lock in the service, because two Suchaks may legitimately hold two engagements on the same candidate and both may claim a `marriage` rung. `marriage` is therefore **removed from the generic `POST …/collaborations/{id}/stages` key list** — one rung, one door. **The rung and this row are ONE transaction** (`claimStage()` nests inside `record()`'s, so it is a savepoint): a refused recording used to leave a `marriage` rung standing, and `SuchakSuccessFeeTrancheService` releases on settled RUNGS and never reads this table, so one confirmation on that orphan released the whole success fee with nobody named. The other half of that invariant is in `SuchakCollaborationService::confirmStage()`, which refuses to confirm a `marriage` rung with no LIVE row here. |
| **WHEN THE WEDDING HAPPENED**, as distinct from when it was reported | `suchak_marriage_outcomes.married_on` (**a DATE**) | `SuchakMarriageOutcomeService::record()`, from the recorder's input — never `now()` | `married_on` (request and response) | **Four clocks, and only the first is the marriage:** `married_on` (the wedding), `suchak_marriage_outcomes.created_at` (row written), `suchak_collaboration_stage_events.claimed_at` (a Suchak reported it) and `.confirmed_at` (the family confirmed it). M3 keys the cross-Suchak share on "a fixed number of days after a recorded Marriage", so running it from any reporting instant would pay a helper **later** the longer the claim was suppressed — the exact thing M3's own sentence forbids. **Do not add a date column to `suchak_collaboration_stage_events`** (a साखरपुडा date, when it is wanted, is a deliberate decision with its own actor rule, not a generic nullable column meaningless on 13 of 14 rungs). The deadline itself is **arithmetic, never stored**: `SuchakMarriageOutcome::shareFallsDueAt()` = `married_on + SHARE_DUE_DAYS_AFTER_MARRIAGE` (30, the only home for that figure), correct on a production where `schedule:run` never fires — the same terminal-fact-plus-arithmetic discipline as `SuchakVisitConfirmation::isClaimLapsed()`. Refused at the door: **no date at all** (the rule used to live only in the controller's `['required','date']`, and `Carbon::parse('')` answers NOW, so an omitted field silently recorded TODAY as the wedding), a **future** date (§7.4/D25 — every tranche is released by an event that already happened), a date **before the engagement opened** (§6.2 credits the introduction), and — the one that is about WHO TYPED IT — a date older than `SHARE_DUE_DAYS_AFTER_MARRIAGE` **when the recorder is the HELPING Suchak** (`assertRecorderMaySetThisDate()`). `marriage` is `CLAIMANT_EITHER_SUCHAK`, so without that rule the party M3 owes the share TO could name a date already past his own deadline and hold an overdue claim the second it was written. The customer-owning Suchak may still name any past day inside the engagement's life: he is the party who OWES the share, so an old date runs his own clock out sooner — a statement against his own interest, and exactly how M3's "suppressing the record must ACCELERATE the obligation" is meant to work. |
| **That an admin SET A MARRIAGE ATTRIBUTION ASIDE, and why** (§6.2's correction door) | `suchak_marriage_outcomes.void_seq` (0 = live, own id once voided — the **only** thing the four unique indexes read) + `voided_at` + `voided_by_user_id` + `void_reason` | `SuchakMarriageOutcomeService::voidClaim()` — admin only, the only writer | `POST /api/v1/admin/suchak/marriage-outcomes/{outcome}/void` (`SuchakAdminTerminalStageApiController`, behind the `admin` middleware), read back as `is_voided` / `voided_at` / `void_reason` on the attribution card | §6.2 opens with "two Suchaks can hold simultaneously valid representations … on the same candidate", so a rival may claim a marriage on his own engagement — and a bare candidate UNIQUE made that first UNCONFIRMED claim **permanent**: rows here are undeletable and no correction path existed, so the largest sum in the system was decided by who tapped first. **`void_seq` is a uniqueness carrier, not a second answer to "is it void"** — MySQL has no partial unique index and SQLite's is not portable through the schema builder, so each unique is `(key, void_seq)` and the model's `saving` guard refuses any row where `void_seq` and `voided_at` disagree. **Rows are never deleted; they stop counting.** The model carries a **global scope** (`SuchakMarriageOutcome::SCOPE_LIVE`) so every existing reader — including `SuchakCrossSuchakObligationService`'s unordered `->first()` — sees only live rows; `includingVoided()` is the deliberate opt-out and the correction door is its only caller. **The door never opens on a CONFIRMED claim** (D26's confirmation is what made it the attribution) and requires a stated reason. The rival's `marriage` rung survives on purpose: `confirmStage()` refuses to confirm a `marriage` rung with no live row here, so it cannot turn into money, and the same engagement can re-record against it. |
| Whether a marriage claim was CONFIRMED | **Derived, no column on `suchak_marriage_outcomes`.** `SuchakMarriageOutcome::isConfirmed()` reads its own `stageEvent->confirmed_at` | `SuchakCollaborationService::confirmStage()` — unchanged, still the only writer | `POST /api/v1/suchak-engagements/{collaboration}/stages/confirm` (member API) | **Never copy `confirmed_at` onto the outcome row** — D26's claim→confirm pattern keeps one home, and a copy is a second answer free to disagree. The outcome row exists **from the claim**, unconfirmed, on purpose: M3's cross-Suchak share must not be killable by silence, while M4 keeps the CUSTOMER's own fee waiting for the customer. **Who may confirm now lives in the SERVICE** (`confirmationActorType()`), not only in `MemberSuchakStageApiController` — see trap 15. The docblock's "an admin may confirm in their place" was **false when it was written**: the member door 404s anybody who is not one of the two candidates, so the ACTOR_ADMIN branch was unreachable and §2's family with no login had nobody who could confirm at all. `POST /api/v1/admin/suchak-engagements/{collaboration}/stages/confirm` (`SuchakAdminTerminalStageApiController`) is what makes the sentence true, and it records ACTOR_ADMIN — never ACTOR_USER — so no trail says the family spoke when an administrator spoke for them. |
| **Is a success-fee share owed under the 12-month anti-circumvention clause, and until when** (D11, D21, 9a A5/A6/A13) | **Derived, no column and no table.** `SuchakTwelveMonthClauseService` reads the `viewed` rows (`CLAUSE_ANCHOR_STAGE`) whose engagement's commission agreement names a customer agreement revision on this `customer_context_id`. Anchor = `claimed_at`; window = `BINDING_MONTHS` (12); releases = `prior_acquaintance_declared` (A6), the calendar-month ordinal past `BINDING_VIEWS_PER_CALENDAR_MONTH` (A5), and lapse | Not written — computed on the read path. **No timer, no queued job, no `schedule:run` dependency**: lapse is evaluated when asked, the way `SuchakRequestPipelineService` and `SuchakMarketplaceChallengeService` already hedge, because production may not run the scheduler at all and the notifications/governance queues have had no worker since 2026-06-17 | `GET /api/v1/suchak/customer-contexts/{customerContext}/twelve-month-clause` (every binding) and `GET .../twelve-month-clause/{candidate}` (one pair) — owning Suchak only, 404 otherwise; the **family** reads the same clause on `GET /suchak/customer-portal/{token}/stages` | **Added 2026-08-03; nothing of it existed before — no constant, no column, no code.** Keyed on `suchak_customer_contexts`, **never** on the agreement or the engagement, and that is D21 itself: "leaving does not void the clause", so a rejected/cancelled engagement and a superseded/declined agreement revision all keep their binding. The reader therefore has **no `status` filter and no `terms_status` filter**, and that absence IS the rule — adding either would delete D21 silently. Owed to `suchak_customer_contexts.suchak_account_id` (read off the context, not the engagement's role label, which vanishes with the engagement). `BINDING_VIEWS_PER_CALENDAR_MONTH = 10` is **the one product number the blueprint does not fix** — A5 says only "a monthly cap", unlike §7.2's stop-loss which names "2 claims, or ₹5,000"; it is one constant, set generously because releasing a genuine obligation is the expensive direction. **Read only — no money moves.** `success_fee` reports the frozen `suchak_service_packages.post_marriage_fee_amount` through `MoneyFormat::amount()` so the answer can say how much; the marriage outcome, success attribution and owed-vs-paid ledger are Phase 4. |
| Which candidate profile's own user may CONFIRM or DISPUTE a meeting | Derived, no column: `SuchakVisitConfirmationService::customerSideMatrimonyProfileId()` reads `suchak_visit_confirmations.customer_context_id` → `suchak_customer_contexts.candidate_matrimony_profile_id`, falling back to `requesting_matrimony_profile_id` only when the meeting names no customer context | Not written — read by `assertCustomerSideUserCanConfirm()` (behind `confirmByUser()` and `disputeVisit()`) and by `MemberSuchakMeetingApiController::viewerContext()` | `POST /api/v1/suchak-meetings/{visit}/confirm`, `POST /api/v1/suchak-meetings/{visit}/dispute` | **Re-pointed 2026-08-03 from direction to ROLE.** The guard hard-coded `requesting_matrimony_profile_id`, which is a DIRECTION; in a marketplace meeting the Suchak answering a challenge becomes the requester (§5.2 direction note), so that column names the **HELPER's** candidate. The wrong family could confirm a fee the customer's family owes and the fee-bearing family could not confirm at all — M4 says no fee falls due without **the customer's** confirmation. `customer_context_id` is written at schedule time from the payment context of the **arranging** Suchak, so its candidate is by construction the family whose Suchak arranged the meeting and whose agreement `fee_amount` was quoted from. **Nothing was widened** — the permitted set is still exactly one profile's own user; the fallback covers meetings that name no customer context, where nobody is billed under an agreement. Do not re-derive this question at any other call site: two copies is exactly how the controller and the service drifted apart in the first place. **SECOND ROLE SOURCE, 2026-08-05 — the ENGAGEMENT, consulted before the directional fallback.** Once an accepted engagement could open a pipeline (see "WHERE A PIPELINE CAME FROM"), the fallback stopped being harmless: nothing in the marketplace flow resolves a platform payment context on that pipeline, so `customer_context_id` is null on an ordinary marketplace meeting and the fallback is exactly what it reaches — naming the HELPER's candidate, whose own member login could then have confirmed a meeting arranged for the customer's family, recorded as the customer's answer. `pipeline.collaborationRequest` is asked instead, through `hasRecordedCustomerOwner()` + `customerOwnerMatrimonyProfileId()` — a recorded fact, never the `customer_owner_side` default. The directional fallback survives beneath it, unchanged, for member-born meetings. Still exactly one profile's own user at every step. **2026-08-06 — the first source now actually fires.** `customer_context_id` is no longer null on an ordinary pipeline: `scheduleVisit()` falls back to the pipeline's representation (see "What THIS ONE meeting costs"), so the party who CONFIRMS and the party who is BILLED are read off the SAME context instead of two different ones. On a member-born meeting that moves the confirming party from the requesting member to the represented candidate's family — which is the correction, not a regression: the member who approached holds no agreement and owes no per-meeting fee, and M4 means the family that pays. |
| How the success fee is split into installments, and what each installment has earned so far | `suchak_success_fee_tranches` (`SuchakSuccessFeeTranche`), one row per tranche, hanging off `customer_agreement_id` | **PLAN columns** — `SuchakSuccessFeeTrancheService` (rules T1–T4 + the arithmetic) with `SuchakAgreementService::createAgreementSnapshot()` as the only writer. **LEDGER columns** — `SuchakSuccessFeeTrancheService::release()` (the three `released_*`) and `::settle()` (`customer_payment_id` + `settled_at`), plus the rung-keyed carry-forward in `SuchakAgreementService::persistTranchePlan()` | `GET/POST /api/v1/suchak/collaborations/{collaboration}/success-fee-tranches[/release]`, `POST …/{tranche}/settlement` | **The success-fee AMOUNT is not here.** It stays on `suchak_service_packages.post_marriage_fee_mode` + `post_marriage_fee_amount`; this table only says how that one figure is cut, so there is no second success fee to drift. A plan is refused unless the mode is `fixed` with a positive amount. **No amount column, deliberately** — the rupee figure is derived once in `SuchakSuccessFeeTrancheService::amounts()` (integer paise): T1 every share is a percent OF THE TOTAL, T2 the last row is the REMAINDER so the parts sum to the whole exactly, T3 the declared shares must sum to 100.00 (checked in hundredths-of-a-percent integers — "exactly" cannot be enforced with a float tolerance), T4 first-smallest is ADVISORY, returned by `advisories()` and logged, never blocked. `trigger_stage_key` is constrained in PHP to `SuchakCollaborationStageEvent::STAGE_LADDER` and must ascend that ladder — free text is forbidden, and out of order "first" and "final" stop meaning anything. **Owned by the AGREEMENT, never by an engagement — that is M9**: the plan columns are inside `agreement_snapshot_hash` (so re-cutting a split after acceptance reads as "terms changed", exactly like editing the price or either meeting fee — backfill `2026_08_01_171000`), while `released_by_collaboration_request_id` / `released_by_stage_event_id` / `released_at` / `customer_payment_id` / `settled_at` are ledger state OUTSIDE the hash. A broken settlement therefore never takes a paid tranche with it, a later match fires only the unpaid rows, attribution is per tranche (blueprint 7.4, so helper A's declared share applies to A's tranche), and the family's exposure stays at the one agreed figure. A revision carries the plan forward with its release/payment state, **keyed by `trigger_stage_key` and carried unconditionally** — the first version copied it only when the plan was byte-identical, so one percent of difference anywhere wrote every new row with all five ledger columns NULL, a clean slate that exposed the family to the whole fee twice. `assertPlanChangeAllowed()` is **per committed row**, not per plan: a committed rung may not be deleted, re-cut or demoted from the remainder, and the refusal names it in Marathi; the instalments that have NOT fired stay re-shapeable, which is §7.4's own sentence ("the paid tranche stands; only the UNPAID tranches fire on the new match") and what a whole-plan veto would have broken the moment a release existed. `customer_payment_id` is a pointer to `suchak_customer_payments`, never a copy of its amount or status; one payment settles exactly one tranche. |
| **WHICH RUNG RELEASED AN INSTALMENT, AND WHEN** (M10 + §7.4 per-tranche attribution) | `suchak_success_fee_tranches.released_by_collaboration_request_id` + `released_by_stage_event_id` + `released_at` | `SuchakSuccessFeeTrancheService::release()` — the ONLY originator. Until phase 4 nothing assigned these at all (the sole assignment was the copy-forward above), so `isReleased()` / `isSettled()` / `isCommitted()` could only ever return false and every rule reading them was inert | `POST /api/v1/suchak/collaborations/{collaboration}/success-fee-tranches/release`; read on the sibling `GET` as `released_by_stage_key` / `released_by_later_stage` | **`released_at` is the instant the RUNG SETTLED, never `now()`.** The whole release is derived from `SuchakCollaborationStageEvent::isSettled()` (the three `CONFIRMABLE_STAGES` need `confirmed_at`, the rest settle on the claim — that predicate is bound, never restated), so a release recorded a year late carries the same date as one recorded the same afternoon, and the `GET` answers correctly even if the writer never ran. Production may not invoke `schedule:run` and two queues have had no worker since 2026-06-17 — the same terminal-fact-plus-arithmetic discipline as `SuchakVisitConfirmation::isClaimLapsed()`. **M10** — an unreleased tranche takes the EARLIEST settled rung at or after its own trigger, so a wedding recorded without a साखरपुडा releases both, each row naming the wedding as what released it. **The cascade stops at `SuchakSuccessFeeTrancheService::LAST_RELEASING_STAGE` (= `marriage`)**: `share_settled` sits later on the ladder, settles on the claim, needs no confirmation and is markable by the HELPER alone — the party being paid — and the ladder does not enforce monotonic progress, so allowing it would fire the entire plan on one unconfirmed tap (9a A7). **First writer wins**: a tranche already released keeps its engagement and its rung even when a later engagement re-derives an entitlement to it, because re-attributing would hand helper B the fruit of helper A's work (§7.4). Releases target the **latest** agreement revision on the package chain, not the revision the engagement is bound to, and are refused entirely while `isTermsSatisfied()` is false on it. |
| **M9 at the CUSTOMER level — "paid once per customer, in total"** | **Derived, no column and no table.** TWO units, both enforced at release time. (a) MONEY — `SuchakSuccessFeeTrancheService::committedMoneyOnOtherChains()` prices, through `amounts()`, every committed tranche on the family's OTHER `service_package_id` chains and subtracts it from THIS chain's own agreed total; a tranche fires only if it still fits in what is left. (b) RUNG — `committedStagesOnOtherChains()` keeps one trigger stage from being charged twice. Both walk `siblingLedgerAgreements()`, which takes the LATEST revision per chain | Not written — enforced in `entitlement()` and published on the ledger `GET` | `other_chain_commitments[]`, `other_chain_committed_display`, `family_allowance_display` | **The stage key alone guarded NOTHING against disjoint plans.** Basic [लग्न ठरले 10%, साखरपुडा 90%] and Premium [विवाह 100%], each quoting ₹1,00,000, share no trigger — so both chains released in full and one family was charged **₹2,00,000**. M9 is about the family's MONEY, not the rung, so the money cap is the rule and the rung check is the narrower one beside it. **LATEST REVISION PER CHAIN is load-bearing for the money and was not for the stages**: `persistTranchePlan()` copies release/settlement state forward, so a committed tranche exists once per revision — a set absorbs the duplicates, a SUM does not. **What this still cannot do:** two chains may quote two DIFFERENT totals and nothing owns "this customer's success fee", so the family's ceiling is the LARGER of the two, never the sum. Fixing that needs a `customer_context`-level success fee that the schema does not carry. Do not add a `customer_context_id` column to the tranche table to "fix" it: that fact is already reachable through `customer_agreement_id`, and the reconciliation needs an owner one level up, not a copied key. Cross-SUCHAK is out of scope — `customer_context_id` is per Suchak account and M1 says each customer pays only their own. |
| **WHICH RUNGS MAY RELEASE A SUCCESS-FEE TRANCHE** (§7.4's three releasing events, D26, M4) | **Derived, no column.** `SuchakSuccessFeeTrancheService::releasingStages()` — the window on `SuchakCollaborationStageEvent::STAGE_LADDER` between `FIRST_RELEASING_STAGE` (`marriage_settled`) and `LAST_RELEASING_STAGE` (`marriage`) | Enforced twice: `normalizePlan()` refuses the plan, `settledReleasingRungs()` refuses the rung. `SuchakPaymentSetupApiController` validates `Rule::in(releasingStages())` — bound, not a second list | `first_releasing_stage_key` / `last_releasing_stage_key` on the ledger `GET`; `blocked_reason` on a row planned outside the window | **The cap alone was not a guard — the FLOOR was missing.** Every rung below `marriage_settled` settles ON THE CLAIM (`isSettled()` needs `confirmed_at` only for the three `CONFIRMABLE_STAGES`), so a plan of [`meeting_scheduled` 100%] released the WHOLE success fee on one Suchak's unconfirmed tap — M4, D25 and §7.4's refund argument at once, and `assertPlanChangeAllowed()` then froze the row forever. The window comes out as `CONFIRMABLE_STAGES` and a test pins that, so §7.4's three and D26's three can never drift apart in silence. A tranche stored outside the window before this guard existed never fires **and now says why** — silence was the other half of the same defect. |
| **WHETHER A RECEIPT MAY SETTLE A TRANCHE, AND HOW MANY** (M10's cascade, M9's paid predicate, M3 half A) | **Derived, no column.** `SuchakSuccessFeeTrancheService::settle()` treats `suchak_customer_payments.amount_received` as a BUDGET: a tranche settles only if its own amount (priced by `amounts()` through `pricedLedger()`) still fits in the receipt minus the tranches already bound to it **on the same agreement revision** | `SuchakSuccessFeeTrancheService::settle()` — the only writer of `customer_payment_id` / `settled_at` | `POST …/success-fee-tranches/{tranche}/settlement`; refusal names the instalment, the receipt and what is left, in `MoneyFormat` | **Nothing compared the receipt to the amount**, so a **₹1** receipt set `settled_at` on a **₹50,000** instalment and the ledger read ₹50,000 settled — and `settled_at` is M9's paid predicate AND M3's half A, so the family read as paid and the cross-Suchak share fell due at once. The rule written to stop that ("one payment, one tranche") broke M10's headline case instead: a wedding cascades three instalments, the family pays the whole fee in ONE receipt, and only one could ever be marked paid. **Scoped to the agreement revision** because `persistTranchePlan()` copies `customer_payment_id` forward — an unscoped count exhausts a receipt that was never spent. |
| Which meeting this is for a pair, and whether it is a re-visit | `suchak_visit_confirmations.meeting_sequence` (1, 2, 3... within one `pipeline_id`) | `SuchakVisitConfirmationService::scheduleVisit()` — the only writer | `meeting_sequence` (Suchak schedule response, member confirm/dispute response) | **The old `unique(pipeline_id)` is GONE** (migration `2026_08_01_190000`) — it meant one meeting per pair forever, which is why D24 had no re-visit to charge for. The replacement guarantee is `unique(pipeline_id, meeting_sequence)`, so two concurrent schedules cannot both become meeting 2; the DB refuses the second. A plain `pipeline_id` index was added FIRST because MySQL will not drop the last index a foreign key can use. `meeting_sequence > 1` is the ONLY definition of a re-visit — do not add an `is_revisit` flag. Not a ladder position: `suchak_collaboration_requests.marketplace_stage` records `meeting_scheduled` once per engagement, this counts the meetings under it. |
| Whether a meeting was held offline or online | `suchak_visit_confirmations.meeting_mode` ∈ `SuchakVisitConfirmation::MEETING_MODES` (`offline`, `online`) | `SuchakVisitConfirmationService::scheduleVisit()` | `meeting_mode` (accepted on `POST /api/v1/suchak/meetings`, echoed on every meeting payload) | Constants, never free strings — the mode PICKS the rate, and a typo would silently freeze the wrong money. Defaults to `offline` when the caller says nothing, because that is what every meeting recorded before this column existed was. D2: the two rates are fully independent amounts, so the mode cannot be inferred from the fee and the fee cannot be derived from the other rate. |
| What THIS ONE meeting costs | `suchak_visit_confirmations.fee_amount`, frozen at schedule time | `SuchakVisitConfirmationService::scheduleVisit()` (resolves the accepted agreement via `customer_context_id`, then the mode picks the rate) | `fee_amount` (raw) + `fee_display` (`MoneyFormat::amount()`) | **NOT a duplicate of the rates.** `suchak_service_packages.per_meeting_fee_amount` / `per_meeting_online_fee_amount` are the RATE on the customer’s accepted agreement — one figure describing the whole relationship; this is the AMOUNT CHARGED for one meeting. Section 4 makes a rate change a new agreement revision, and a meeting already held must not silently re-price when that happens. Rate → agreement → this meeting are three homes for three different questions. **D24: the sequence number plays no part in the calculation** — a re-visit costs exactly what the first visit cost, no discount and no escalation. NULL is a real answer (no customer context, no agreement in force, or a rate never quoted) and must never be printed as ₹0. **WHERE `customer_context_id` COMES FROM — corrected 2026-08-06.** It used to come from ONE place: a `suchak_payment_contexts` row keyed on this pipeline. **Nothing in production writes one** — neither pipeline creator makes a payment context, `SuchakPaymentCollectorResolver` only does so when a Suchak records a manual ledger payment, and `SuchakLeadAllocationService`'s row carries a NULL `pipeline_id` that a pipeline-keyed lookup can never see. So every meeting the app could schedule froze `fee_amount = NULL`: schedulable, completable, confirmable by the family, and then no figure on D17's approval screen and no fee due under M4/M5. `SuchakVisitConfirmationService::pipelineCustomerContextId()` now falls back to `suchak_pipelines.representation_id` → `suchak_customer_contexts.representation_id`, which carries `unique(representation_id)` — **at most one customer per representation, so there is nothing to guess between**, unlike the two agreed PLANS `soleAgreementInForce()` must refuse. It is also the same context `GET /customers/{representation}/payment-request-options` resolves, so the plan the app names and the customer the server prices from are now provably the same one. The payment context stays FIRST; this never overrides it. Account mismatch and `isClosedForPayment()` both return null rather than pricing. **No `fee_currency` column, deliberately** — the currency is the accepted agreement’s. **D17: never sum these for a screen a family is deciding on.** The running total belongs on the payments screen; beside an approval it reads as a regret ledger. |
| Whose candidate was met, when it was not the arranging Suchak’s own | `suchak_visit_confirmations.helper_suchak_account_id` (nullable FK → `suchak_accounts`) | `SuchakVisitConfirmationService::scheduleVisit()` | `helper_suchak_account_id` (accepted on schedule) | `suchak_account_id` on the same row already names the Suchak who ARRANGED the meeting; this names the other side of a marketplace meeting. Refused when it equals `suchak_account_id` — that value would make the column say something untrue. NULL on an ordinary meeting. **AMENDED 2026-08-05.** This row used to say the helper was "not derivable today" because a visit hangs off `suchak_pipelines` and the pipeline had no link to `suchak_collaboration_requests`. It now can have one (`suchak_pipelines.collaboration_request_id`, the row below), so on an ENGAGEMENT-born pipeline the helper IS derivable — `pipeline.collaborationRequest->helpingSuchakAccountId()`. The column stays, and is still the only thing `visitDisputeActorType()` consults, because it remains the ONLY answer on a member-born meeting and because a derived value could disagree with what was recorded at schedule time. **Do not write both and do not add a second link on the visit row**: if the two ever disagree the recorded one wins, and the honest next step is to make `scheduleVisit()` default it from the engagement rather than to duplicate the fact. **DONE 2026-08-06 — that is now what happens.** `helperSuchakAccountId()` returns `engagementHelperSuchakAccountId($pipeline)` when the caller sends nothing; an explicitly sent value still wins and both routes pass the same refusal. It became necessary the moment the app could schedule the FIRST meeting on an engagement-born pipeline, which it does with `pipeline_id` and `meeting_mode` and nothing else — the `awaiting_first_meeting` payload publishes the other Suchak's NAME and not his key, on purpose. Left null, `visitDisputeActorType()` matched nobody and §7.2's stop-loss, whose whole premise is unanswered HELPER claims, would have attached to no marketplace meeting the app can create. Nothing widened — the dispute door already admitted the helper; only the column it reads now says who that is. |
| **WHERE A PIPELINE CAME FROM** — a member's request, or an accepted engagement | `suchak_pipelines.request_id` (FK → `suchak_profile_requests`, now nullable) **or** `suchak_pipelines.collaboration_request_id` (nullable FK → `suchak_collaboration_requests`, `unique`) — exactly one is set, `SuchakPipeline::ORIGIN_COLUMNS` + `assertExactlyOneOrigin()` on `saving` | `SuchakRequestPipelineService` and nothing else: `createRequest()` writes the member origin, `openPipelineForEngagement()` writes the engagement origin | No new endpoint — the engagement origin is written as a side effect of `POST /api/v1/suchak/collaborations/{collaboration}/accept` | **Added 2026-08-05 (blueprint 5.1 / 6.1 / 6a).** The meeting engine hangs entirely off `suchak_pipelines`; `suchak_visit_confirmations` has no `collaboration_request_id` and must not gain one — a meeting reaches its engagement THROUGH its pipeline. A pipeline had one creator (a member's request), so an accepted engagement never had one and the `meeting_scheduled` rung could be claimed with no meeting able to exist behind it (production: 20 pipelines, 0 visit confirmations). **`request_id` is nullable rather than fabricated**: an engagement has no `suchak_profile_requests` row and inventing one would be a forged member approach in the audit trail, read as real by the SLA sweep, the member's sent list and the Suchak inbox. `unique(request_id)` is unchanged and still means one pipeline per member request — MySQL and SQLite both allow repeated NULLs. The engagement column is not a second copy of anything: without it the origin is unnameable, idempotence would have to be guessed from the profile pair (which can legitimately also carry a member-born pipeline), and nothing could answer which engagement a meeting sits under. **`lock_expires_at` is nullable and NULL on this path** — it is the Suchak's REPLY clock, and an accepted engagement has already been answered; a deadline there would put an "SLA risk / Review request" card in front of the arranging Suchak pointing at a request that does not exist. `isPastSla()`, `expireDuePipelines()` and `slaRisks()` all already treat NULL as "no clock". **Every column is filled by ROLE, never by direction** — the engagement stores its pair by direction and in the marketplace the responder is the requester, so `selected_suchak_account_id` / `target_matrimony_profile_id` / `representation_id` come from `customerOwnerSuchakAccountId()` / `customerOwnerMatrimonyProfileId()` / `customerOwnerRepresentationId()`. **No pipeline is opened until the customer-owning role is a RECORDED fact** (`SuchakCollaborationRequest::hasRecordedCustomerOwner()` — the same gate `assertStageClaimant()` applies, extracted to the model so the two cannot drift); `customer_owner_side` defaults to `target`, so reading it alone proves nothing. **No backfill**: the 20 existing pipelines and the existing collaborations are untouched, and an engagement accepted before this shipped has no pipeline. |
| The CHALLENGE — "I hold this customer; I will pay X to whoever brings the match" | `suchak_marketplace_challenges` (`SuchakMarketplaceChallenge`), one row per publication | `SuchakMarketplaceChallengeService` (`publish()` / `withdraw()` / `expireDue()`) — the only writer | `POST /api/v1/suchak/marketplace/challenges`, `POST .../{challenge}/withdraw`, `GET .../challenges` (browse), `GET .../challenges/mine`, `GET .../challenges/{challenge}` (logged open) | **Verified 2026-08-02 that nothing could hold it, and the reason is decisive: `suchak_collaboration_requests` carries BOTH account ids NOT NULL from row one, and a challenge by definition has no counterparty yet** — it is published to invite one. Same for every other candidate owner. Not to be confused with `SuchakPublicMarketplaceService` / `PublicMarketplaceController`, which are the PUBLIC WEBSITE DIRECTORY OF SUCHAK ACCOUNTS — no candidate, no share. **It is the INVITATION, never the engagement**: once a helper proposes and the publisher accepts, the existing `suchak_collaboration_requests` + `suchak_commission_agreements` pair IS the engagement (blueprint 6.1) and this row points at it. **It carries NO copy of the candidate's visible facts** — only `representation_id`; every read goes through `SuchakCandidateMaskingService::maskedSummary` so D19a's four defaults and the originating Suchak's per-candidate reveals apply live and cannot go stale in a second copy. `customer_agreement_id` is NOT an input: §4 says *"publication attaches to whichever agreement is accepted at that moment"*, so the service resolves the latest ACCEPTED revision on the candidate's customer context and freezes it — that is what makes A8 enforceable. At most ONE `open` challenge per representation, enforced under a row lock in the service (no portable partial unique index); two live challenges at two shares is A8's escape hatch. **No `chargeable_ceiling`** despite blueprint §5.5 still listing one — D17 and §15 record the product owner reversing that idea twice. **No `times_published` counter** — it is `count(*)` over this table per representation (A12). `STATUS_FULFILLED` is written by `SuchakCollaborationService::acceptRequest()` (2026-08-03) — the moment the publisher accepts a proposal made against it, which is the only honest one. It is written from the collaboration service and not from this one because this service already depends on that one (it calls `claimCustomerStage()` to record publication) and calling back would be a cycle; the lifecycle POLICY stays here. Silent when the challenge is no longer open, and other PENDING proposals are deliberately left pending — they are the publisher's to reject or let expire. |
| The DECLARED SHARE — what the publisher will pay a helper, decided upfront | `suchak_marketplace_challenges.declared_share_type` + `declared_share_percent` **or** `declared_share_amount` | `SuchakMarketplaceChallengeService::publish()` | `declared_share.{type,percent,amount,currency,display,success_fee_display,estimated_share_display}` | **`suchak_commission_agreements.groom_side_share` / `bride_side_share` look like this and are NOT** — they are a TWO-WAY split of an EXISTING pair that must sum to 100 and name both sides by account id; a challenge has no second side. So the **vocabulary** is bound (`SuchakMarketplaceChallenge::DECLARED_SHARE_TYPES` references `SuchakCommissionAgreement::SPLIT_CUSTOM_PERCENT` / `SPLIT_FIXED_AMOUNT` — no new strings, so the commission agreement written on acceptance can carry the same words) while the **columns** are the challenge's own, one-directional ones. `SPLIT_TO_BE_DISCUSSED` is excluded by D4 — the share is not negotiable by the helper, so "to be discussed" is the one thing a challenge cannot say; `SPLIT_EQUAL_PERCENT` is `custom_percent` at 50.00 and a second spelling of one number. Exactly one of percent/amount is set, guarded in `assertDeclaredShare()` on `saving` (same reason as the stage ladder's owner guard: no portable CHECK). **No success-fee amount here** — it stays on `suchak_service_packages.post_marriage_fee_amount`; a PERCENT declaration is refused unless that is `fixed` with a positive amount (same rule as `SuchakSuccessFeeTrancheService::assertPackageCarriesFixedSuccessFee()`, since a percent of `as_wished`/`none` has no total), and `estimated_share_display` is arithmetic at read time, never a stored second figure. **And no currency column either — the same answer the meeting fee already gave one row above.** A first draft of `2026_08_02_200000` carried `share_currency` (3 chars, default `INR`), accepted `share_currency` on the publish route behind a `size:3` rule, and rendered it. That made it the THIRD owner of one fact and it did not merely go stale: with an INR agreement whose package carried `post_marriage_fee_amount = 100000`, publishing `declared_share_percent=30` with `share_currency=USD` passed every guard and `listingPayload()` then rendered `success_fee_display = "USD 1,00,000"` and `estimated_share_display = "USD 30,000"` — the publisher's own rupees, relabelled as dollars, on the screen another Suchak uses to decide whether the work is worth doing. **The owner is `suchak_service_packages.currency`; `suchak_customer_agreements.currency` is its frozen snapshot** (copied at proposal time by `SuchakAgreementService`, inside `agreement_snapshot_hash`, and immovable by a re-quote — `SuchakPackageCatalogService::applyPlanTerms` "can never move a package's name, scope, currency or publish state", and `normalizedPlanTerms()` states the rule outright: *"the package currency is already settled by the caller — a fee can never carry another"*). A share is a SLICE of that money, so it is spent in that money; the challenge already points at the agreement through `customer_agreement_id`, and the value is READ through `SuchakMarketplaceChallenge::declaredShareCurrency()` (agreement → package → `INR`), never stored. The publish route now **prohibits** `share_currency` / `currency` rather than dropping them silently. Migration `2026_08_02_200000` was edited in place because it had not run anywhere; do not add a currency column back for "future multi-currency" — that case needs the package and the agreement to move first, and this row follows them. |
| When a challenge stops being live, and why | `suchak_marketplace_challenges.status` + `expires_at` / `withdrawn_at` + `withdrawn_reason` / `fulfilled_at` | `SuchakMarketplaceChallengeService::withdraw()` / `expireDue()` | `status`, `expires_at`, `expires_never`, `withdrawn_reason` | **`expires_at` is NULLABLE here and NOT NULL on `suchak_collaboration_requests` — the difference is the point.** A collaboration request's expiry is always `SuchakPolicyService::collaborationSlaDays()` because it is a named counterparty's deadline to answer; a challenge has no counterparty to hold to a deadline, so its expiry is the publisher's own decision and NULL means "open until I withdraw it", a real answer rather than a missing one. **Do not apply `collaborationSlaDays()` here.** Withdrawal never deletes: A7's realized-vs-declared ratio and A8's twelve-month stickiness both read declarations a publisher would prefer gone, so `delete()` throws and `withdrawn_reason` records the stated reason. Expiry is swept on the browse/own-list read paths (the pattern `expireForAccount()` set), not only by a scheduler. |
| "Another Suchak OPENED my listing" | `suchak_activity_logs` with `action_type = marketplace_listing_opened` | `SuchakMarketplaceChallengeService::openListing()` | — | **The first READ this log has ever recorded** — every other action is a write (a request created, a consent verified, a payment recorded). D18 requires it ("every listing open is logged and shown to the originating Suchak"), and borrowing `collaboration_request_created` or any other write action would put "someone looked" and "someone did" in the same bucket, which is the one distinction D18 exists to make. `suchak_account_id` on the row is the **ORIGINATING** Suchak, because the log is shown to HIM; the viewer travels as `actor_user_id` plus `viewer_suchak_account_id` in `metadata_json`. **A browse is not an open** — the index read is deliberately not logged per card, because twelve rows per scroll would bury the signal. Publish / withdraw / expire have their own three actions beside it (`marketplace_challenge_published` / `_withdrawn` / `_expired`); withdrawal and expiry are separate because A8 names withdrawal as an attack and an expiry the publisher chose in advance is not that. |
| **"SUCHAK A OWES SUCHAK B"** — the cross-Suchak obligation (M2, M3, 9a A7) | `suchak_cross_suchak_obligations` (`SuchakCrossSuchakObligation`): `payer_suchak_account_id` + `payee_suchak_account_id`, its ORIGIN (`collaboration_request_id`, `marriage_outcome_id`, `marketplace_challenge_id`, nullable `success_fee_tranche_id`), a FROZEN `amount` + `currency`, and its settlement (`settled_at`, `settled_by_user_id`, `settlement_reference`, `settlement_note` — **all four READ back in `payload()`**; a settlement column with a writer and no reader is evidence nobody can be held to) | `SuchakCrossSuchakObligationService::raise()` / `settle()` — the only writer | `POST`/`GET /api/v1/suchak/collaborations/{collaboration}/cross-suchak-obligations`, `GET /api/v1/suchak/cross-suchak-obligations`, `POST /api/v1/suchak/cross-suchak-obligations/{obligation}/settle` | **Before 2026-08-04 NOTHING in this schema could name a payer, and four tables looked as if they could.** `suchak_platform_payouts` is platform → ONE Suchak; `suchak_customer_payments` is customer → ONE Suchak and cannot even say WHICH fee it paid; `suchak_ledger_entries` has the entire receivable vocabulary (`TYPE_SUCCESS_FEE_EXPECTED`, expected/due/paid/waived, `due_date`, `paid_at`, a `collaboration_request_id`) but ONE `suchak_account_id`, a **NOT NULL `matrimony_profile_id`** and a **hand-typed `entry_type`** chosen from a `Rule::in` dropdown on two Blade forms (`CrmLedgerController`, `CollaborationController`) — a debt whose existence depends on a human picking the right string; `suchak_commission_agreements` is acceptance-only (pending/accepted/rejected/cancelled), and its `groom_side_share` / `bride_side_share` are a two-way CREDIT split summing to 100, not a one-directional debt. **A payer column on the ledger entry was rejected** on three properties of that row: the NOT NULL candidate (a share is not owed by a family), the human-typed type, and the fact that every existing reader — CRM ledger screen, dashboards, `SuchakIncomeAnalyticsService`, `SuchakWorklistSourceQueries` — would silently start counting cross-Suchak debts as customer receivables. **Scope is M2 and is deliberately narrow:** `marketplace_challenge_id` is NOT NULL, so a DIRECT collaboration raises nothing (D5 — "a Suchak who declared nothing owes nothing"). **The grain is the TRANCHE** (§7.4 — "attribution is recorded per tranche, not per customer"), with NULL meaning the agreement carries no installment plan; M10 credits this engagement with every tranche not already released against a different one. **Raising is idempotent and open to BOTH Suchaks** — that is M3 at the door: if only the payer could raise his own debt, doing nothing would erase it. **AN OBLIGATION MAY NOT EXIST AHEAD OF THE FEE IT IS A SHARE OF, and `raise()` enforces that with two gates that own nothing themselves.** GATE 1: the `marriage` rung must be SETTLED — `SuchakCollaborationStageEvent::isSettled()`, and `marriage` is a `CONFIRMABLE_STAGE`, so that means the customer (or an admin standing in) CONFIRMED it. Without it the payee records the wedding himself (`CLAIMANT_EITHER_SUCHAK`), types the date, raises his own share, and the payer is publicly overdue on A7 thirty days later for a fee whose release is stuck on the same unconfirmed rung — M1 broken by making A pay B out of pocket rather than by a platform float. GATE 2: the tranche must carry a release instant in `SuchakSuccessFeeTrancheService::entitlement()` — ONE call that carries every release rule (trigger rung settled or M10-cascaded, LIVE revision's terms accepted, M9 other-chain block, and **a trigger above `LAST_RELEASING_STAGE` that can never release at all**), none of them restated here. **THE HONEST GAP:** §2's family has no login, so on those engagements gate 1 passes only when an ADMIN confirms; until Phase 6's OTP (D23) there is no third route, and if nobody confirms, no obligation is raised. That is deliberate — an honest gap beats a debt nobody owes, and `delete()` throws so a wrongly-raised row could never be withdrawn. **WHICH REVISION: THE LIVE ONE.** `entitlement()` resolves through `ledgerAgreementFor()` (latest revision on the same `service_package_id`), which is where release and settlement actually write. Reading the revision the §6.2 row is BOUND to pointed every obligation at rows that will never release or settle: M3 half A permanently null while `customer_payment_is_answerable` still said true, and the §7.4 attribution skip testing dead rows — handing helper B the tranche helper A released. The ROW invariant is the CHAIN, not the revision (`assertMatchesItsOrigin()` compares `service_package_id` and, where both carry one, `customer_context_id`): pinning it to the live revision would make an already-correct row unsaveable the moment a later revision landed, so `settle()` would start throwing on rows raised yesterday. **The `amount` is FROZEN and that is not a duplicate of the declaration:** a live-derived figure would depend on `suchak_service_packages.post_marriage_fee_amount`, which the PAYER may edit for the life of the package, and the agreement freezes it only inside `agreement_snapshot_hash` — a digest no rupee can be read back from. `unique(marriage_outcome_id, success_fee_tranche_id)`; the NULL-tranche half is closed under the engagement row lock in `raise()`, because both MySQL and SQLite treat a tuple containing NULL as distinct. |
| The RUPEE VALUE of a declared share | **Derived, one owner:** `SuchakMarketplaceChallenge::declaredShareTotal()` — a fixed declaration as-is, a percent declaration against `suchak_service_packages.post_marriage_fee_amount` in `MODE_FIXED`, **NULL** when that base does not exist | — (a read) | `declared_share.estimated_share_display` on every listing; the frozen `amount` on each obligation | Added 2026-08-04 beside `declaredShareCurrency()`, which exists for the identical reason. The arithmetic used to sit inline inside `SuchakMarketplaceChallengeService::declaredSharePayload()`; the obligation needs the same answer to FREEZE, and a listing quoting ₹30,000 while the debt quotes something else is one promise with two numbers. NULL rather than 0 — a percent of `as_wished`/`none` is not a number, and 0 would print a promise of ₹0 to a browsing Suchak. |
| **Whether a cross-Suchak share HAS FALLEN DUE** (M3) | **Derived, no column and no scheduler.** `SuchakCrossSuchakObligation::fallsDueAt()` = the EARLIER of `customerPaidAt()` (half A) and `marriageClockDueAt()` (half B); `isDue()` / `isOverdue()` / `overdueDays()` / `dueReason()` beside it | — (arithmetic on every read) | `falls_due_at`, `customer_paid_at`, `customer_payment_is_answerable`, `marriage_clock_due_at`, `due_reason`, `is_due`, `is_overdue`, `overdue_days` | **Half A has ONE owner and it is not the payment table:** `suchak_success_fee_tranches.settled_at` (+ `customer_payment_id`, written by `SuchakSuccessFeeTrancheService::settle()`) is the only place this schema can say a customer payment WAS the success fee. **Do not add `fee_type` to `suchak_customer_payments`** — it would be a second home for that fact. Where an agreement carries no installment plan there is no such pointer, so half A is **unanswerable**, reported as `customer_payment_is_answerable: false` rather than guessed, and M3 degenerates to half B — which still guarantees the obligation exists. **Half B is the ACCELERATION M3 demands:** it runs from `suchak_marriage_outcomes.married_on` (the WEDDING DAY) plus `SuchakMarriageOutcome::SHARE_DUE_DAYS_AFTER_MARRIAGE` (30, its only home), so a marriage suppressed for six months and recorded late produces an obligation ALREADY overdue on the day it is raised — and the `marriage` rung is `CLAIMANT_EITHER_SUCHAK`, so the payee can start the clock himself. **But he cannot start it ALREADY EXPIRED, and he cannot start it ALONE.** Not expired: `assertRecorderMaySetThisDate()` refuses a `married_on` older than `SHARE_DUE_DAYS_AFTER_MARRIAGE` when the recorder is the HELPING Suchak, so the payee may start his clock but never hand himself one that has already run out; a genuinely older wedding is recorded by the Suchak who OWES the share, whose old date is a statement against his own interest. Not alone: `raise()` requires that rung to be SETTLED, and `marriage` is confirmable, so the clock runs from a wedding somebody other than the claimant agreed happened. Otherwise the payee would type both the fact and its deadline — half B would accelerate a debt that no released tranche stood behind. Acceleration is preserved exactly: confirmation does not reset `married_on`, so a wedding confirmed late is overdue the day the obligation is raised. **No stored `due_at` and no `status` string:** a copy of `married_on + 30` is the first thing to go stale when the constant moves, and a status is a second answer to a question two timestamps already give. Correct on a production where `schedule:run` never fires (same discipline as `SuchakVisitConfirmation::isClaimLapsed()`). |
| **The realized-vs-declared ratio on a declarer’s card** (9a A7) | **Derived, no column.** `SuchakCrossSuchakObligationService::declarerRatio()` — settled amount ÷ declared amount, computed PER CURRENCY over `suchak_cross_suchak_obligations` where the account is the PAYER | — (a read) | `GET /api/v1/suchak/cross-suchak-obligations/ratio` (own) and `.../ratio/{suchakAccount}` (another declarer, verified callers only) | Before 2026-08-04 `grep -i realized` over `app/` returned **four comments and zero code**, and `STAGE_SHARE_SETTLED` was claimable and **read by nothing**. **Denominator is obligations RAISED *plus* the ones that WOULD be raised, never challenges published** — D5 says an unanswered declaration costs nothing, and counting publications would punish a Suchak for a market that did not answer. **THE ACCOUNT LEDGER TAKES THE SAME DERIVATION, IN BOTH DIRECTIONS (fixed 2026-08-04).** `ledgerFor()` read `owedBy()` / `owedTo()` — stored rows ONLY — while this ratio, rendered as a card directly above that list on the same screen, counted stored rows PLUS derived ones. Confirmed by hand on a device against production data: "जाहीर: 3 · पूर्ण: 0 · उशीर: 2" printed an inch above "अजून एकही आंतर-सूचक नोंद नाही", about one Suchak's money, with two of those shares already overdue — the payer had no row to raise and the payee no row to chase, so the judged party could not act on the number judging him. `derivedUnraisedObligations()` now takes a `$role` (`DERIVED_FOR_PAYER` / `DERIVED_FOR_PAYEE`) and the ledger asks it once per direction — **one routine asked a different question, NEVER a second derivation**; the ratio still asks only the payer question, because a ratio is about a declarer's own promises. Dedup is against the union of BOTH directions' stored rows, so a row and its derived twin can never both reach `totals()` and inflate a sum two businesses will argue about. A derived row is marked `is_derived` and carries `obligation_id: 0` (unsaved model, `(int) null`), which is the settle guard's second half: `POST /cross-suchak-obligations/0/settle` cannot bind and 404s, and RAISE — keyed on `collaboration_request_id`, idempotent, open to both sides — is the verb a derived row answers to. **`forEngagement()` still reads stored rows only and is deliberately NOT changed here**; it publishes the same `is_derived` key, which is therefore always `false` on that door. **DERIVE-THEN-RECORD, the Phase 3 discipline the ledger dropped:** a denominator that only exists once somebody presses `raise()` is one the judged party controls by inaction — the declarer never raises his own debt and a helper who stopped chasing does not either, so fifty unpaid shares rendered as `is_new: true`, the NEW badge, on the worst payer's card. `derivedUnraisedObligations()` therefore walks this declarer's marketplace engagements carrying a CONFIRMED marriage and asks `collectibleSlices()` — the very routine `raise()` persists, so there is one arithmetic and not two — for the slices no stored row covers, returning them as UNSAVED model instances so `isSettled()` / `isOverdue()` stay in the row that owns M3. A stored row always wins over its derived twin (keyed on marriage + tranche, the unique index's own grain), the payload states `recorded_obligation_count` and `derived_obligation_count` separately rather than blending them, and the read writes nothing. **The ROLE is the model's, never the query's:** the SQL narrows by participation on the two directional columns and `isCustomerOwner()` decides who the declarer is — reading `target_suchak_account_id` as "the payer" is backwards on every challenge answered by proposing. **D13: a declarer with no obligations is `is_new: true` with `realized_ratio_percent: null`, NEVER 0%** — zero out of zero rendered as 0% is a defamation with arithmetic behind it. Per currency, because a ratio of amounts in two currencies is not a number. The row is **undeletable** (`delete()` throws) so the party the ratio judges cannot prune his own denominator. Settling the LAST obligation on an engagement claims the `share_settled` rung through `SuchakCollaborationService::claimStage()` — the one stage writer — so 6a’s loop finally closes; a rung that cannot be claimed is logged and never costs the settlement, and the already-recorded case is decided by RE-READING, never by matching the exception text. |
| **What the platform can ENFORCE about a cross-Suchak share** (§7.3, M1) | **Nothing, and the read says so.** `SuchakCrossSuchakObligationService::overdueExposureFor()` returns counts, sums and `platform_enforces: false` | — (a read) | `platform_exposure` inside `GET /api/v1/suchak/cross-suchak-obligations/ratio` | **It reads RECORDED rows only and deliberately does NOT take A7's derived fallback:** a public number may be derived, a lever over somebody's money may only act on a debt someone actually raised. §7.3 is precise: *"the platform does not stand between a customer and a Suchak for Suchak-earned fees. It DOES control Suchak payouts."* A cross-Suchak share moves Suchak→Suchak off-platform, so the only lever is money the platform already owes the PAYER (`suchak_platform_payouts` + `SuchakPayoutHold`) — and **a payer with no pending payout has no balance to hold against, which is the normal case for a helper-owed share**. So this service creates **no payout hold, no feature freeze and no ledger row**: it publishes the exposure and a gate that wants to act reads it. M1 forbids more — there is no shared pot and the platform guarantees nothing. **Do not wire an automatic hold here without a product decision**; it would be a platform guarantee over money the platform never touches. |
| When a meeting claim went unanswered, and whether it still counts | `suchak_visit_confirmations.claim_unanswered_since` (one nullable timestamp, stamped once, **never cleared**) + `suchak_visit_confirmations.claim_lapsed_at` (the terminal fact, also never cleared) + the readers `hasUnansweredClaim()` / `isClaimLapsed()` / `claimLapsesAt()` / `scopeUnansweredClaims()` | `SuchakVisitConfirmationService::openSilenceDispute()` writes `claim_unanswered_since`; `SuchakVisitConfirmationService::recordClaimLapseIfDue()` writes `claim_lapsed_at`, called from `confirmByUser()` / `disputeVisit()` / `settleDisputedVisits()` — the three paths that can record an ANSWER — so the fact lands before anything can erase it | `unanswered_claims` on the marketplace listing reads (`openListing`, `published`) — the raw counter §7.2 clause 2 puts on the originating Suchak's card | **Blueprint §7.2, added 2026-08-03.** Nothing already on the row could carry this: `visit_status = disputed` is temporary, `dispute_id` cannot say WHY the dispute opened (a family who contested on day 2 has one too), and `refund_review_status = closed_no_finding` cannot tell the lapse from an admin filing a case away. Clause 5 says the family's window and the originating Suchak's run **in parallel from delivery**, so they expire together and ONE timestamp is both. It is never cleared because clause 4 says a lapsed claim is "still counted" — M3: doing nothing must never make an obligation disappear, and a counter a Suchak can reset by stonewalling for 90 days is not a stop-loss. Whether a claim is still unanswered TODAY is a separate, derived question (`hasUnansweredClaim()` — answered = the family confirmed or contested, or a review reached `upheld`/`dismissed`; `closed_no_finding` is NOT an answer). **`claim_lapsed_at` was omitted at first and that was the defect (reversed 2026-08-03, same day).** The reasoning for omitting it — the 90-day lapse is arithmetic on `claim_unanswered_since`, so "never due" holds on a production where `schedule:run` never fires — is still true and is still how `isClaimLapsed()` falls back. What it missed is that the arithmetic had TWO moving halves: "unanswered AND past 90 days". A family answering on day 99 made the first half false, so the claim stopped being lapsed retroactively, the payout guard let it through, and `hasUnansweredClaim()` dropped it out of the stop-loss — i.e. a Suchak cleared his own counter by stonewalling for 90 days and then collecting one late answer, the exact thing M3 forbids and this row's own note declared impossible. Lapsing is a THING THAT HAPPENED, so it is recorded: `claim_lapsed_at` is stamped with `claimLapsesAt()` (the instant the window closed, **never `now()`** — a fact noticed late still happened on time), is never cleared, and is read FIRST by `isClaimLapsed()`. It is not derivable from `claim_unanswered_since` alone, which is why it is not a duplicate of it: the derivation also needs to know WHEN the claim was answered, and this row cannot say — `user_confirmed_at` covers only one of the ways to answer, a family's contest writes no timestamp at all, and a finding's instant lives on `suchak_disputes.resolved_at`, across a join the counter deliberately does not make. Both guarantees now hold at once: with no job ever running the arithmetic still answers "never due", and once anything touches the row the fact is on it permanently. `hasUnansweredClaim()` is therefore `! isClaimAnswered() || isClaimLapsed()` — clause 4's "still counted", in code. **The counter reads from the VISIT side, never the dispute side** — `suchak_disputes` has no FK back to the meeting, and one is NOT added: every column the count needs (account ids, `fee_amount`, `dispute_id`) is already on this one indexed row. |
| The §7.2 numbers — 7 days, 90 days, 2 claims, ₹5,000 | `SuchakVisitConfirmation::CLAIM_SILENCE_WINDOW_DAYS` / `CLAIM_LAPSE_DAYS` / `STOP_LOSS_UNANSWERED_CLAIMS` / `STOP_LOSS_UNANSWERED_AMOUNT` | Constants — read by the timer, the stop-loss gate and the payout guard | — | Read from the blueprint, never invented, and kept on the model that owns the columns rather than in the sweep service, because three callers need them and a second copy is how two of them end up disagreeing about how long seven days is. The amount is a decimal STRING and INR: ₹5,000 is a rupee threshold, so a non-rupee meeting still counts toward the 2-claim leg but is never added into a rupee total. A change to any of the four is a blueprint change request, and `SuchakSilenceTimerTest` pins all four. |
| **What the platform PAYS a Suchak for a confirmed meeting** | `suchak_growth_reward_rules` rows on `SuchakGrowthRewardRule::TRIGGER_PLATFORM_VISIT_CONFIRMED`, resolved by `SuchakGrowthRewardRule::visitRewardInForce()` | published by an admin through `SuchakGrowthRewardService::createRewardRule()`, reached from `POST admin/suchak/visits/reward-rule`; consumed by `SuchakVisitConfirmationService::boundPayoutAmount()` | — | **This is NOT `suchak_visit_confirmations.fee_amount` and must never be bound to it.** That column is the CUSTOMER-side quote the Suchak set for himself; this is PLATFORM money paid TO that Suchak, and binding them would make the payee own the platform's obligation (the defect the empty amount box on the meetings screen was an interim guard against). Reuses the existing reward-rule engine rather than adding a second price list — no new table, no new column, no migration: `reward_trigger` is a plain `string(64)` with no enum. **`attribution_policy` is meaningless on a visit-trigger row** (it belongs to the referral engine and a meeting has no attribution); nothing reads it there, and `SuchakGrowthRewardService::assertRuleQualifies()` — previously a tautology, now load-bearing — is what stops a visit rule ever being paid as a referral reward. **`is_active` now has a writer that can make it false: `SuchakGrowthRewardService::withdrawRewardRule()`, reached from `POST admin/suchak/visits/reward-rule/{rewardRule}/withdraw`.** Before it, `is_active` had two readers (`scopeInForce()`, `assertRuleQualifies()`) and one writer that hard-codes `true`, so a published meeting price could be superseded but never WITHDRAWN — supersession can only say "the price is different", never "the platform stops paying for meetings". Withdrawal touches `is_active` and NOTHING else: the amount, currency, window and key stay unrewritable and a payout already qualified reads back at the figure it was qualified at. It is ONE-WAY (a second call is refused) because un-withdrawing rewrites what the platform said, and publishing a fresh rule is the honest way back. Safe to leave un-timestamped only because both readers are ever asked about NOW. Otherwise rules are immutable and undeletable, so a price change is a NEW row and the newest in force wins (dated beats undated, later `starts_at` beats earlier, higher id breaks the tie); the applied `rule_key` is written onto the payout's visit event so the figure can be read back a year later. |
| **The ceiling on a TYPED meeting payout, and who may qualify one alone** | `SuchakPolicyService::KEY_SUCHAK_VISIT_PAYOUT_MAX_AMOUNT` (default 7500) + `KEY_SUCHAK_VISIT_PAYOUT_REQUIRES_SECOND_ADMIN` (default false) | read by `SuchakVisitConfirmationService::boundPayoutAmount()` / `assertPayoutActorAllowed()` | — | The ceiling is the INTERIM, live only while no visit reward rule is published; a published rule beats it and is never capped by it, or an old default would silently overrule a deliberate decision that carries its own audit row. 7500 is derived: above every meeting reward this codebase qualifies (fixtures run 750–6000) and below 10,000, tranche 1 of the ₹1,00,000 success fee in blueprint §7.4, so a Phase-4-sized figure cannot be typed into a meeting form. Four-eyes is OFF by default because the default `user_and_admin` policy REQUIRES an admin confirmation before a payout can qualify, so a single-admin deployment would deadlock on every meeting; what makes that acceptable is that the amount is no longer the admin's to choose. The compensating record is written either way — `single_actor_qualification`, `admin_confirmed_by_user_id`, `payout_qualified_by_user_id` in both `suchak_visit_confirmation_events.metadata_json` and the admin audit row, plus a badge on the meetings row. **Neither key is on the Suchak settings screen yet** — both are tunable only by inserting the `suchak_policies` row directly. |
| **The rung shape a marketplace ladder event is published as** | `App\Modules\Suchak\Services\SuchakStageLadderPresenter::rung()` — `stage_event_id`, `stage_key`, `stage_label`, `owner`, `collaboration_id`, `customer_agreement_id`, `claimed_at`, `confirmed_at`, `claimant`, `requires_confirmation`, `is_settled` | the presenter alone; `SuchakCollaborationStagesApiController::stagePayload()` delegates to it | `data` on both claim doors; `stage_events[]` on `GET /suchak/collaborations` | **Added 2026-08-02.** The shape existed only as a private method on the claim controller, so the collaborations LIST read (which now carries an engagement's recorded rungs) would have needed a second copy — and the client a second parser for one fact. **Do not restate it anywhere.** It deliberately withholds `event_note` (free text that can name a family) and every `claimed_by_*` column: the list read reaches a cross-Suchak counterparty's rungs, and neither is a ladder fact. |
| **Whether an engagement's customer-owner ROLE is a recorded fact** | `suchak_commission_agreements.customer_agreement_id` being non-NULL — `SuchakCollaborationService::bindCustomerAgreement()` is the ONE writer and writes it together with `suchak_collaboration_requests.customer_owner_side` | `linkCustomerAgreement()` (direct path) and `createRequest()` (marketplace path) | `customer_agreement_id`, `customer_owner_side`, `is_customer_owner` on `GET /suchak/collaborations`; `data.customer_agreement_id` on the link door | **`customer_owner_side` DEFAULTS to `target`.** On an unlinked engagement it is a column default, not a finding — `assertStageClaimant()` reads exactly this predicate before trusting a role, and `SuchakCollaborationsApiController` publishes `customer_owner_side` / `is_customer_owner` as **null** until it holds. Never publish the default as though it were a fact. |
| **Which representation a Suchak may reach his own customer's agreements through, from an engagement** | `suchak_collaboration_requests.requesting_representation_id` / `target_representation_id`, read for the CALLER'S OWN side via `sideForAccount()` | the engagement's creation (`createRequest`, marketplace `propose`) | `my_representation_id` on `GET /suchak/collaborations`; `customer_owner_representation_id` (the same id under the role's name, **null unless the caller is the owner**) | **Added 2026-08-02** to make `linkCustomerAgreement` reachable: it needs a `customer_agreement_id`, whose only lister is `GET /customers/{representation}/payment-request-options`, which needs a representation id. **Never publish the counterparty's representation id** — it is a handle to another Suchak's customer (D19a) and `payment-request-options` 404s on it anyway. |
| **Which recorded receipt may be bound to a success-fee instalment** | `suchak_customer_payments` rows scoped on `suchak_account_id` — the identical predicate `SuchakSuccessFeeTrancheApiController::settle()` applies to the row it is handed | `SuchakCustomerPaymentService::recordManualPayment()` (unchanged — this is a READ) | `GET /suchak/customer-payments` → `customer_payments[].id`, filterable by `customer_agreement_id`; `is_bound_to_tranche` / `bound_tranches[]` | **Added 2026-08-02.** No route ever listed this id space: `/suchak/payments` returns `suchak_ledger_entries`, `/suchak/payment-requests` returns `suchak_payment_requests` ids (a DIFFERENT space — sending one binds the wrong receipt to a family's money) and `customer_payment_id` reached the app only in the one-shot `mark-paid` body. **No settlement rule is restated on this read** — whether a tranche still fits inside a receipt is `SuchakSuccessFeeTrancheService::settle()`'s budget arithmetic, and no tranche amount is priced here (`amounts()` is the one owner). Unpaid rows are LISTED with their status, never filtered out. |
| **A Suchak's behavioural record** (§11 phase 5 — engagements, meetings, terminal claims, marriages, challenges) | **Derived, NO COLUMN AND NO TABLE.** `SuchakReputationService::record()` counts it on every read from the rows the engines already write | — (a read) | `GET /api/v1/suchak/reputation` (own) and `GET /api/v1/suchak/reputation/{suchakAccount}` (another declarer's, verified callers only) | **Do not add a stored score, a counter table or a nightly rollup.** §5.5 is explicit — *"add a READER over existing event tables, not a new event log"* — and a stored score is stale the instant a meeting is confirmed or a share settled, which under the frozen no-duplicate rule makes it a second home for a fact the events already carry. **Do not extend `SuchakQualityControlService`** either: §12 forbids a second 0-100 score and names the confusion outright — that one is an admin RESTRICTION risk score. **Two blocks are BOUND, not recomputed:** `declared_share` = `SuchakCrossSuchakObligationService::declarerRatio()`, `unanswered_claims` = `SuchakClaimSilenceService::unansweredClaimSummaryAfterSweep()`. A second computation of either would be a second answer to a number the blueprint publishes once. **IT IS A DISCLOSURE (§9 shows it to every Suchak), so the payload names nobody:** no candidate, no family, no village, no district, no per-event date, no free text, and no `matrimony_profile_id` / `representation_id` / `customer_context_id` / `collaboration_request_id` / `challenge_id`. `SuchakCandidateMaskingService` is deliberately NOT called — it is the presenter for a CANDIDATE, and this read presents none. The only identity on the card is the SUCHAK's own (`suchak_account_id`, `suchak_name`, `is_verified` — the three publisher keys `SuchakMarketplaceChallengeService::listingPayload()` already publishes to the same audience). `SuchakReputationReadTest::test_no_candidate_identity_reaches_a_reputation_payload` pins the whole key list, so a field added later without a decision trips over it. |
| **Whether a proportion may be published at all** | `SuchakReputationService::MIN_RATE_DENOMINATOR` (= **5**) + `SUPPRESSED_NO_EVENTS` / `SUPPRESSED_TOO_FEW_EVENTS`; every rate travels as `{percent, numerator, denominator, threshold, is_publishable, suppressed_reason}`. The FORMATTING of the number is not this constant's — it is `App\Support\PercentDisplay::rate()` at `DECIMALS_RATE`, the one percent formatter | — (a read) | every `*_rate` key on the reputation card | **D13: NEVER `0%` FOR A NEWCOMER.** An empty denominator returns `percent: null` with `no_events`, never `0` — zero out of zero rendered as 0% is a defamation with arithmetic behind it, and it lands on the card of the Suchak who joined today. **Small denominators lie:** one dispute out of one meeting is not a 100% dispute rate, so a rate over fewer than five events is withheld (`too_few_events`) while the COUNTS still ship from event one — which is §7.2 clause 2's own trade, *"a raw count from the first event beats a ratio that needs volume to move"*. **Five, not ten:** below five one event moves the rate by twenty points or more (larger than any real difference the figure could be used to tell apart), and above five a marketplace event is a year of somebody's work, so a threshold of ten would leave almost every genuine Suchak permanently rate-less — D13's unemployability arriving one step later. Move this constant and every rate moves with it; do not hard-code a second threshold anywhere. |
| **A customer's own trail — what was arranged, what happened, what was cancelled** (D20) | **Derived, NO COLUMN AND NO TABLE.** `SuchakCustomerHistoryService::forCustomer()`, keyed on `suchak_customer_contexts.id` | — (a read) | `GET /api/v1/suchak/customer-contexts/{customerContext}/history` | D20: *"facts only, derived from records, NEVER TYPED BY A SUCHAK"* — enforced by construction, because there is no writer here at all and every source column is written by the engine that owns it. **Owner-scoped: 404 for any other Suchak, never 403** (403 would confirm the family exists). **TWO OF D20's OWN FOUR FIGURES DO NOT EXIST ON THIS PLATFORM AND THE PAYLOAD SAYS SO:** (a) *"cancelled by the family"* is impossible — `SuchakVisitConfirmationService::cancelVisit()` admits the arranging Suchak or an admin and refuses the member outright (M5 gives the family `dispute` instead) — so `cancellations.by_family` is **`null`, never `0`**, with `coverage.family_cancellation_recorded: false` beside it; (b) attendance is still missing (§5.1 B4 — no attendance column, no `actual_held_at`, no no-show event), so nothing here claims it and the nearest true fact ships under its own name, `meetings.scheduled_past_date`, with `coverage.attendance_recorded: false`. **Do not add an `attended` or `no_show` key without a column behind it** — a Suchak who has not got round to marking a meeting complete produces the identical row to a family who did not turn up, and publishing the two as one fact puts a no-show on a family's record for a Suchak's paperwork. **NO MONEY** (D17). **`stops_at_marriage` closes on a CONFIRMED marriage, never on a claim** — closing a family's record on one Suchak's word would let a claim nobody agreed to end their history. |
| **Every answer the market gave for ONE of my candidates** (§11 phase 5, §16 "per-candidate proposal inbox") | **Derived, NO COLUMN AND NO TABLE.** `SuchakCandidateProposalInboxService::inboxFor()` gathers `suchak_collaboration_requests` across every `suchak_marketplace_challenges` row whose `representation_id` is this candidate — withdrawn and expired challenges included (A8: the share sticks to candidates already suggested under it) | — (a read) | `GET /api/v1/suchak/marketplace/candidates/{representation}/proposals` | **The row is `SuchakMarketplaceChallengeService::proposalPayload()` VERBATIM**, which is what carries the masking — the proposed candidate goes through `SuchakCandidateMaskingService::maskedSummary()` in there, so D19a's four defaults and the proposing Suchak's per-candidate reveals apply the instant he changes them. **Do not assemble a candidate payload here and do not add a second masking path** (S6). Pinned by `SuchakCandidateProposalInboxTest::test_the_inbox_candidate_payload_is_byte_for_byte_the_marketplaces_own_masked_summary`, which compares the emitted block to the presenter's own output rather than to a list of fields. **The DECLARED SHARE is published once per challenge, never per row** — the same refusal `proposalsFor()` makes, and this read has the most reason to obey it because its rows genuinely come from challenges at different shares. Own candidate header is UNMASKED (masking is for other Suchaks' candidates), and the three facts on it come from the masking service's shared public readers, not a private copy. Gated on the VERIFICATION badge (D18) **and** ownership — owning the candidate is not a substitute for the badge, the exact hole `proposalsFor()` records having had. |
| **Whether a way of narrowing a cross-Suchak list is allowed at all** | `SuchakCrossSearchService::OWN_BOOK_ONLY_FILTERS` + `CROSS_SEARCH_NARROWEST_FILTERABLE_HIERARCHY` (unchanged, and reached from outside that class through the new pass-through `applyCrossSuchakProfileFilters()`), plus `SuchakCandidateProposalInboxService::SORTS` for the ordering half | — | the inbox query string | **THE RULE IS ONE: a surface may narrow only by a fact the reader could already have READ on it.** Filters were already covered; the phase-5 inbox added the SORT axis, which is the same oracle asked sideways — an ordering is a comparison, so sorting by income names who earns more without printing a figure, and sorting by village names the grouping. `SORTS` therefore carries only `recent` / `oldest` / `age_asc` / `age_desc` / `fit_desc`: a proposal fact, an age the masked card prints, and the CAPPED fit score `/suchak/search` already publishes beside every masked card. **No location sort, no income sort, no name sort — and an unknown sort falls back silently**, never 422, because a refusal that names the refused key is a map of what is worth probing for. `match_field_points` is deliberately not emitted at all. Pinned by `SuchakCandidateProposalInboxTest` (income, name, village-id, sort list, and the trap-14 score check). |
| **What the marketplace looks like to somebody deciding whether to publish** (§11 phase 5, §9 "market economics") | **Derived, NO COLUMN, NO TABLE AND NO CACHE.** `SuchakMarketEconomicsService::marketFor()` over `suchak_marketplace_challenges` + `suchak_collaboration_requests` + `suchak_marriage_outcomes` | — (a read) | `GET /api/v1/suchak/marketplace/economics` | §9's matrix grants "another customer's fees" to VERIFIED Suchaks and to nobody else, so the badge is the gate (D18/A10). **Derive, never denormalize:** a stored answered-count is wrong the moment a proposal is withdrawn and a stored share median is wrong the moment a challenge is republished. **NO PARAMETERS, deliberately** — every knob is a way of narrowing an aggregate onto a smaller population until its denominator is one Suchak the reader already picked out of the browse list. The bound on the row load is `WINDOW_DAYS` (365) and it is a PRODUCT bound (a share declared two years ago is not evidence about today's market), reported in the payload; `open_now` is deliberately outside it. Money through `MoneyFormat`, percentages through `PercentDisplay` — no client re-derives either. |
| **Whether an AGGREGATE over other Suchaks may be published at all** | `SuchakMarketEconomicsService::MIN_OBSERVATIONS` (= **5**) **and** `MIN_DISTINCT_PUBLISHERS` (= **5**), both enforced in the single `block()` helper every figure passes through | — (a read) | `observations`, `publishers`, `is_withheld`, `withheld_reason` on every block | **The same 5 as `SuchakReputationService::MIN_RATE_DENOMINATOR`** — one platform, one answer to "how thin is too thin"; two numbers would tell two stories about the same market on adjacent screens. **BOTH halves are needed:** fifty challenges from one Suchak are one Suchak's terms (rows are cheap to manufacture), and five publishers with one challenge each are five figures a reader can line up against five browsable listings. **THE VIEWER'S OWN ROWS ARE EXCLUDED FROM EVERY SET** so he can never subtract himself out of an aggregate he is inside — which also makes the read mean "the market I would publish into", matching `browse()`. **MEDIAN ONLY — no minimum, maximum or quartile ever**, because at n=5 an endpoint IS one Suchak's declared share printed exactly; a mean is likewise refused (over five values, knowing four recovers the fifth). Keys survive a withholding with `null` values so a client has a stable shape; `observations`/`publishers` survive too, because they count participation and never terms, and without them "withheld" is indistinguishable from "empty". **`open_now` is the one block never withheld** — its predicate is `browse()`'s exactly, so every row counted is a row the same viewer can already page through. Pinned by `SuchakMarketEconomicsTest`. |
| **Company facts printed on the five public legal pages** (legal entity name, LLPIN, GSTIN, statutory registered office, jurisdiction city, grievance officer name/designation/email/phone/hours, statutory response timelines, refund and retention windows, and the terms/privacy/refund/disclaimer/grievance document versions + effective date) | `config/legal.php` — one file, one fill-in point | the product owner, by hand | none (rendered only, no API) | **Added 2026-08-05 with the five public legal pages.** An unfilled value stays as a literal `[[TOKEN]]` and renders on the page, so a missing fact cannot ship unnoticed; `LegalDocument::unfilledPlaceholders()` lists them and the page shows an amber strip to an admin (never to the public). **Not a duplicate of `App\Services\SiteIdentityService`** — that owns BRAND identity (site name, logo, tagline, social links, support/sales/info email, primary phone, display address) and stays the owner of all of it. `LegalDocument::replacements()` is the single join point and the precedence is one rule: **config wins; Site Identity fills a placeholder only.** The one deliberate exception is the address — `site_identity_address` is a free-form marketing footer string (it literally held `Old Mumbai Road` when this was built) and is **never** allowed to stand in for `entity.registered_address`, because publishing a display string as an LLP's statutory registered office on five legal pages is a fabricated legal fact. **`versions.terms` / `versions.privacy` are NOT free to edit**: the literal string lands in `user_consents.version` and today comes from the CLIENTS, not the server — `flutter-apk/lib/core/app_consent.dart` (`AppConsent::version`) and `Suchak-apk/lib/core/auth/auth_repository.dart` (`consentVersion`), both `2026-06-24`, and the backend validates length only. Bumping this file without shipping both app builds records consent against a document nobody was shown. Which documents exist is `config('legal.documents')` — one list read by the routes, the renderer and the footer; do not restate it. **Scope widened 2026-08-07 (see the row below): these facts are no longer legal-page-only — every public surface now reads them, through one reader.** |
| **WHAT THIS BUSINESS IS, on every public surface** — legal entity name, statutory registered office, the one public phone (and its `tel:` form), the support email, and the company name a footer prints | **Fact: `config/legal.php`. Reader: `App\Services\SiteIdentityService` — the ONE reader every surface goes through.** `SiteIdentityService::CONFIG_FALLBACKS` is the entire join (key ⇒ config path) and there is no other; `App\Support\LegalDocument::replacements()` remains the legal-page half of the same file | the product owner edits `config/legal.php`. An admin may OVERRIDE only the display subset (`company_name`, `support_email`, `primary_phone`, display `address`) from Admin → App Settings → Company & Contact — `AdminSettingsController::updateAppSettings` → `SiteIdentityService::setText` — and only while the box is non-blank | `SiteIdentityService::all()` → homepage footer, legal-page footer, public Suchak pages, any new public page; `:tokens` on the six legal documents; `SiteIdentityService::primaryPhoneTel()` for a `tel:` href | **Added 2026-08-07, ahead of the PayU merchant review, because the homepage a reviewer opens signed out printed NO phone, NO email and NO company address while `/terms` and `/grievance` printed the real ones from config.** The cause was not a missing fact, it was a second store answering first: `site_identity_support_email`, `_primary_phone`, `_sales_email`, `_info_email` and `_secondary_phone` all EXISTED as `admin_settings` rows holding the empty string, and `$rows->get($key, $default)` returns a row that exists — so a blank row beat the fallback and the footer's `@if (! empty(...))` guards rendered nothing at all. **`all()` now treats blank as absent**: admin override (non-blank only) → `config/legal.php` → the literal in `DEFAULTS`. Clearing an override hands the key back to config; it is not a way to publish an empty string. **`entity.legal_name` and `entity.registered_address` are exposed as READ-ONLY keys** — present in `DEFAULTS` + `CONFIG_FALLBACKS`, deliberately absent from `TEXT_KEYS`, so `setText()` refuses them and no admin form can publish a legal name or registered office that differs from the one in the git diff. **The admin form must render `SiteIdentityService::overrides()`, never `all()`** (that is why `overrides()` exists and why the controller passes `siteIdentityEffective` separately, as placeholder text only): pre-filling a box with an inherited value and pressing Save copies config into `admin_settings` and freezes it there, and the next config edit would then silently change nothing — the exact second owner this row exists to prevent. **An unfilled `[[TOKEN]]` counts as "no fact"** and is never published through this reader; it still renders literally on the legal page, where the admin-only strip is there to catch it. **The display-vs-statutory address split from the row above SURVIVES INTACT**: `LegalDocument` still never reads `site_identity_address`, so a marketing string can never stand in for a registered office. The new fallback runs the safe way only — a BLANK display address publishes the registered office (a real address of the same company), never the reverse. **A non-blank `site_identity_address` still wins on the footer**, which today means local holds `Old Mumbai Road`; clearing that one box is the whole fix and is a product-owner action, not a code change. `DEFAULTS['company_name']` was emptied in the same commit — it held a hand-typed copy of `entity.brand_name`. `sales_email` / `info_email` / `secondary_phone` have NO config fallback on purpose: there is one real email and one real number, and inventing a fallback would print the same address three times in one footer. Values are cached 300s under `SiteIdentityService::CACHE_KEY`, so a config edit lands within five minutes without a deploy step; `setText()`/`setImage()` forget it immediately. |
| Member paid-plan catalog money (MRP + payable) | `plan_terms.price` (MRP) + `plan_terms.selling_price` (payable); mirrored on `plans.price` / `plans.selling_price` from the default billing row | Admin `PlanController` → `PlanTerm::syncAdminTermRows()` | API `price`, `selling_price`, `final_price` (alias of selling), display-only `discount_percent` from `PlanPricing::displayDiscountPercent` | **SSOT since 2026-08-06.** Payable is NEVER `price × (1 − %)`. `discount_percent` columns are **deprecated mirrors** of the display formula only — do not charge from them. Display is whole `% OFF` only — no rupee “save” copy. Suchak retention `discount_percent` is a different product and stays untouched. |
| Frozen subscription-contract grace / leftover-carry window at purchase (Phase 2) | `subscriptions.grace_period_days` + `subscriptions.leftover_quota_carry_window_days` | `PlanSubscriptionTerms::contractTimingAttributesFromPlan()` on subscribe / PayU finalize / create / renew | `PlanSubscriptionTerms::gracePeriodDaysForSubscription` / `leftoverQuotaCarryWindowDaysForSubscription` only | Catalog originals stay on `plans.*`. **Keep intentionally:** catalog writers + free-tier/admin readers of `PlanSubscriptionTerms::gracePeriodDays(Plan)` / `leftoverQuotaCarryWindowDays(Plan)`. Runtime access / entitlement expiry / carry window must NOT re-read live `plans.grace_period_days`. Member UI labels for these values are subscription-contract copy (`lang/*/user_plan.php`), not live-plan copy. |
| Leftover quota carry bag applied on renew/repurchase in grace/carry window (`carry_quota`) | `subscriptions.meta.carry_quota` (feature_key → remaining int) | `SubscriptionService::resolveCarryQuotaFromPreviousSubscription` → written into new/renewed subscription meta | `SubscriptionService::carriedQuotaBonus` / `FeatureUsageService` referral-carry paths | **Phase 3.1:** limits for carry math come only from the **previous** `checkout_snapshot.quota_policies` (never live `plan_quota_policies`). Missing previous snap key → skip that feature (log once). |
| Frozen quota engine contract at purchase (`checkout_snapshot.quota_policies`) | `subscriptions.meta.checkout_snapshot.quota_policies` (complete map of every `PlanQuotaPolicyKeys::ordered()` key) | `PlanQuotaCheckoutSnapshot::forPlan()` on purchase writers; repair migrations / `SubscriptionService` backfills | `PlanQuotaUiSource::policyPayloadsForUser` / `policyPayloadsForSubscription` | **Phase 3.1:** existing **paid** members fail closed when map missing/incomplete (no live catalog fallback). **Keep intentionally:** free / no-sub / admin catalog / purchase-time freeze may still read live `plan_quota_policies`; backfill/repair helpers stay. |
| Frozen quota timing multipliers at purchase (Phase 3A) | `checkout_snapshot.quota_bonus_percent` + `checkout_snapshot.quota_duration_multiplier` | Purchase writers copy from `PlanTerm`; repair `SubscriptionService::backfillCheckoutSnapshotQuotaTiming` | `SubscriptionService::quotaBonusPercentForSubscription` / `quotaDurationMultiplierForSubscription` | Paid runtime reads the frozen snapshot only. Missing keys soft-default **0** / **1.0** with one-shot `Log::warning` — never live `plan_terms`. **Keep intentionally:** purchase-time copy from catalog + backfill. |
| Frozen non-quota member PlanFeature values at purchase (`checkout_snapshot.features`) | `subscriptions.meta.checkout_snapshot.features` (string map) | `PlanQuotaCheckoutSnapshot::forPlan()` → `PlanFeatureContractSource::featuresMapForPlan()` on subscribe / PayU finalize / create / renew; backfill `SubscriptionService::backfillCheckoutSnapshotFeatures` / `PlanFeatureContractSource::backfillCheckoutSnapshotFeatures` | read via `PlanFeatureContractSource::valueForUser()` / `valueForSubscription()` only | Quota keys stay in `checkout_snapshot.quota_policies`. **Keep intentionally:** live `plan_features` = catalog / free tier / purchase-time write. Do not store the same values in `user_entitlements.value_override`. |
| Chat image send gate (`chat_image_messages` / alias `chat_images`) | Frozen value in `checkout_snapshot.features.chat_image_messages` (+ entitlement row as access token only) | Purchase snapshot writer; `EntitlementService::assignFromSubscription` keys from snapshot features map | `SubscriptionService::canUseChatImages` / `ChatMessageService::sendImageMessage` | **Phase 3.1 ownership:** gate on frozen contract feature value, not entitlement-row existence alone — entitlement without frozen `1` must not allow images after a PlanFeature catalog edit. |
| Member search spotlight / priority ranking (`priority_listing`) | `subscriptions.meta.checkout_snapshot.quota_policies.priority_listing` (already inside frozen quota map) | Purchase writers via `PlanQuotaCheckoutSnapshot::forPlan()`; repair `SubscriptionService::backfillCheckoutSnapshotPriorityListing` | `ProfileSearchRankingService::applySpotlightFirst` (JSON on subscription meta) + existing `user_entitlements` / `profile_boosts` paths | **Phase 3C.** Do NOT JOIN live `plan_quota_policies` for ranking. Do not add a denormalized ranking column — reuse the frozen quota payload. Free / no-subscription members: no plan priority from catalog at runtime (boosts/entitlements only). **Keep intentionally:** live catalog still defines purchase-time freeze and admin plan cards; ranking backfill stays. |

---

## 2. Engine registry — one capability, one engine

Before writing a service that does any of these, **use the existing one**.

| Capability | Engine | Notes |
|---|---|---|
| Global module on/off (Feature Flags) | `App\Services\FeatureFlagService` + tables `feature_flags` / `feature_flag_audits` | **THE ONE source of truth for enabling/disabling whole application modules.** Admin → System & Access → Feature Flags. Call sites use `isEnabled($key)` only — never read the table directly, never scatter `app()->environment()` for module gates (env default lives only inside the service as absent-row fallback: production=false, else=true). Seeder uses `firstOrCreate` and never overwrites existing rows. First flag: `showcase_profiles_enabled` (`FeatureFlagKey::SHOWCASE_PROFILES`). Distinct from `AdminSetting` keys like `showcase_profiles_visible_in_search` (search visibility only — do not reuse or rename). Toggle writes an immutable `feature_flag_audits` row (old/new, admin, IP, UA, optional reason) and clears cache immediately. Adding a future module = one DB row + one `isEnabled()` gate. |
| Everything that must die when a password changes | `App\Services\Account\MemberPasswordService` | Rotates `users.remember_token`, deletes every `personal_access_tokens` row for the user except the caller's, deletes their `sessions` rows except the caller's, deletes their `password_reset_tokens` row, then sends `PasswordChangedNotification` (push key `password_changed`). **Device tokens are deliberately NOT deleted** — the other phone must still receive the security alert; it has already lost its API token. |
| Mobile push send (FCM HTTP v1) | `App\Services\Push\FirebasePushService` | The only FCM transport. OAuth2 via `google/auth` with the access token cached for its lifetime (+ a 60 s negative cache after an auth failure, so a revoked key cannot add a failed round trip to every request). Never throws — returns a per-token result array — and prunes `UNREGISTERED` / `INVALID_ARGUMENT` tokens through `DeviceTokenService`. Reads `project_id` out of the service-account JSON rather than config, so "which Firebase project?" has one source of truth. Do not add `kreait/firebase-php`. |
| Should this notification become a push? | `App\Services\Push\PushDispatchService` | THE single decision point, reached only from `App\Listeners\SendPushForDatabaseNotification` (bound to `NotificationSent`, `database` channel only). **Never call push from a business flow** — every database notification already arrives here, so a new feature gets push for free and a type is switched off by an admin toggle, not a deploy. Order: channel master → registry lookup → admin per-type → user per-type → quiet hours → tokens → send. `deliver()` is the seam a future queue job would wrap. |
| What a notification type says on a lock screen, and where a tap goes | `App\Services\Push\PushTypeRegistry` + `lang/{en,mr}/push.php` | The registry row owns the push key, group, deep-link `target`, `data_keys`, which apps may receive it, and the admin default; the lang files own the wording. **Adding a type = one registry row + four lang strings (`label`/`description`/`title`/`body`), nothing else** — no admin-screen edit (the tab renders the registry), no API change, no new APK (both apps render the server's category list). An unregistered notification class is skipped and logged, never pushed. `target` is a CONTRACT both Flutter apps switch on — never rename one. The optional `body_named` / `body_named_preview` strings are the REVEALED-person variants consumed by `PushTeaserCopyService`; a locked row never reaches them (its body is the teaser line). `lang/mr` and `lang/en` must stay key-for-key symmetric — pinned by `ReceivedInterestTeaserAndPushCopyTest`. |
| The WhatsApp text a Suchak relays to a family from his own dashboard | `lang/{en,mr}/suchak.php` → `dashboard.share_card_whatsapp_line`, `dashboard.share_card_tagline`, `dashboard.profile_request_reply_shown_to_family` / `_ask_contact` / `_under_discussion` | The lang files own the wording; `resources/views/suchak/dashboard.blade.php` composes it and holds none of it | The Suchak dashboard's WhatsApp share link and the three one-tap reply buttons on an open member request | **Product owner's decision, 2026-08-05: relayed text follows the SENDER's locale — the Suchak's.** All five strings were hardcoded Marathi, so a Suchak working in English composed Marathi out of his own English screen. The locale is the one `SetLocaleFromQuery` already resolved for the page; nothing here re-decides it, and no `__(..., 'mr')` may pin a language at a call site. **The number stays DATA**: `:number` is substituted and never `number_format`ed, so the frozen Latin-digits rule holds in both languages. **Known limitation, not fixed here**: `SetLocaleFromQuery` falls back to `en` when the session carries no locale and never READS `users.preferred_locale` (it only writes it), so a Suchak whose saved language is Marathi gets English text until he picks a language again in that session. That is the resolver's behaviour on every page, not this text's — changing it is a separate decision. |
| What a push says when there is a PERSON behind the event | `App\Services\Push\PushTeaserCopyService` | **Reflows the teaser, it does not re-derive privacy.** Reads the `teaser` block the notification already stored — output of `WhoViewedTeaserPresenter` under that surface's admin policy — and turns it into one body line, so the push and the in-app card can never disagree about what a member may see. Locked row ⇒ teaser-level only (identity-free by construction: masked/courtesy name, taluka not village, whatever the policy withheld stays withheld). Unlocked row ⇒ the real name, from the explicit `sender_name` / `accepter_name` / `viewer_name` keys the notification writes (never parsed back out of the English `message`). Neither ⇒ returns null and `PushDispatchService` keeps the generic `push.types.*.body` line. Ordering is deliberate — Android clips the body near 40–50 chars, so it is headline (who/where) → lines (age, marital status) → match score last; `viewed_summary`, `interest_hint` and `accent_line` are excluded on purpose. Locked CHAT is the one exception: it uses `ChatTeaserPolicy`'s own strings, because that surface's admin dial defaults to anonymous and did not consent to attributes leaking through a push. Also the single choke point for the FROZEN Latin-digit rule (`normalizeDigits()`, applied to title and body on every path). Do not build a second push-wording path. |
| "Does this table / column exist?" | `App\Support\SchemaPresence` | The ONE process-level memo for `Schema::hasTable` / `hasColumn`. Every optional table and legacy column in this codebase is guarded by one of those, inside per-profile and per-pair code paths — a single production suggestions request issued **1,625 `information_schema` round trips (1.2 s)** re-answering questions whose answer cannot change while the process lives. `MatrimonyProfile`, `ProfileCanonicalResidenceService` and `NearbyGeographyResolver` had grown their own private statics for this; they all delegate here now. Flushed automatically on Laravel's migration events (`AppServiceProvider`), which is the only moment a schema can change mid-process. Do not add a fourth private memo. |
| Derived taluka / district coordinate | `App\Services\Location\GeoCentroidBackfillService` | Owns the median formula, the per-state bounds (`STATE_BOUNDS`, public) and the acceptance gate (`accept()`). The migration and `locations:backfill-geo-centroids` both call it; `locations:audit-geo-centroids` re-applies the same three checks read-only. Change a threshold in ONE place — the service — or the writer and the auditor start disagreeing. |
| Candidate filters on every Suchak search (cross-Suchak AND own book) | `App\Modules\Suchak\Services\SuchakCrossSearchService::applyProfileFilters()` | THE one filter owner — `GET /api/v1/suchak/search`, the web `GET /suchak/search` and the marketplace own-candidate picker (`GET .../my-candidates`, via `ownRepresentationsQuery()`) all pass through it, so `age_min` cannot mean two things. Add a filter here, never in a screen. **A filter may narrow only by a fact the reader can already READ on that surface** — otherwise the result count becomes a read channel for a value the card withheld. `$ownBook` is the switch: `name` and `income_min`/`income_max` (`OWN_BOOK_ONLY_FILTERS`) exist only on the caller's own book, and `district_id`/`taluka_id` refuse an `addresses` id below `CROSS_SEARCH_NARROWEST_FILTERABLE_HIERARCHY` on a cross-Suchak read because `whereResidenceUnderAncestor()` accepts an ancestor at ANY depth. Level is read from `addresses.hierarchy` via `Location::defaultLevelForHierarchy()` — do not write a second hierarchy→level map. Pinned by `SuchakCrossSearchOracleTest`. |
| Marital status keys, dependent fields, year sanity | `App\Support\MaritalDependencyRules` | Canonical status vocabulary. Consumed by web wizard, mobile API and Suchak flow. |
| Minimum marriage age (F18 / M21) | `App\Support\MarriageAgePolicy` | Shared by web wizard, mobile API, Suchak manual create. |
| Fuzzy person-name matching | `App\Support\NameMatcher` | **Three name-comparison engines exist** — this one (fuzzy, scored), `DuplicateDetectionService` (exact snapshot compare), `IntakeDuplicateFieldMatchEvaluator` (intake-to-intake). Extend one; do not add a fourth. Known P2: strips Devanagari vowel marks. |
| Mobile normalisation | `App\Support\MobileNumber` | |
| Contact visibility / masking | `ContactVisibilityDecision`, `ContactVisibilityStrictness`, `SuchakCandidateMaskingService` | |
| Consent contact roles | `App\Support\ConsentContactRole` + `SuchakConsentContactSuggestionService` | |
| Suchak pre-create duplicate check | `SuchakCandidateDuplicateCheckService` | mobile + name + DOB(±1y) + gender scoring, plus optional weak village/caste signal. Tiers: `confirmed`/`high` = app may hard-stop, `medium`/`low` = advisory. Server itself never blocks. Also owns `owner_type` (mine / other_suchak / platform_member / unrepresented) — it reuses `SuchakProfileRepresentation::scopeWithValidConsent()` + `scopePubliclyRoutable()` for "actively represented" and "may name the other Suchak"; do not write a second ownership rule. |
| Suchak write-gate on a represented profile | `SuchakProfileRepresentation::suchakMayEditProfile()` (+ `requiresConsentBeforeSuchakEdit()`, `CONSENT_GATED_EDIT_MODES`) | The ONLY predicate deciding whether a Suchak may write to a represented candidate. Built on the existing `hasValidConsent()`. Enforced once, in `SuchakRepresentedProfileApiController::authorizedContext($forWrite: true)` — every write endpoint funnels through it. Do not re-derive consent state per endpoint. |
| Consent WhatsApp hand-off link | `SuchakConsentService::whatsappShareUrl()` | Single implementation shared by the consent sheet API and the duplicate→link response. |
| Candidate name masking ("Shriram Kadam" → "Shriram K.") | `App\Support\CandidateNameMask` | The ONE name mask for pre-consent surfaces. `SuchakCandidateDuplicateCheckService::maskName()` now delegates to it; the pending-consent-claims feed uses it too. Mobile masking stays `ConsentContactRole::maskMobile()`. Do not hand-roll a third. |
| Is this representation a pending (un-consented) claim? | `SuchakProfileRepresentation::isPendingConsentClaim()` + query twins `scopeExcludingPendingConsentClaims()` / `scopeOnlyPendingConsentClaims()` | The two scopes are exact complements and must stay that way: whatever the customer feeds hide, the consent-requests feed shows — otherwise a claim becomes unreachable and the Suchak can never resend the consent. `excluding…` is used by `SuchakCustomerListService`, the customer-detail and share-card controllers and the web dashboard; `only…` by `SuchakPendingConsentListService`. |
| Suchak pending consent-claim feed | `App\Modules\Suchak\Services\SuchakPendingConsentListService` → `GET /api/v1/suchak/consent-requests` | **Read model, no table.** The complement of `SuchakCustomerListService`: the only surface an un-consented claim is visible on, so the consent-first flow has no dead end. Returns masked name/mobile, the consent id + status, and `consent_link_available: false` always (the raw token is hashed at rest) — a usable link only ever comes from `POST /suchak/consents/{consent}/resend`. It mutates nothing; promotion to a real customer stays `SuchakConsentService::promoteConsentedClaimToCustomer()`. |
| Suchak account approve/reject/suspend/archive | `SuchakAccountLifecycleService` | **The only approval path.** Single and bulk admin actions both route through it so activity logging and guard rails stay identical. |
| Daily distinct-viewer trend (member dashboard 7-day chart) | `App\Services\WhoViewed\ProfileViewTrendService` → `GET /api/v1/matrimony-profile/view-trend` | **Read-only aggregate over `profile_views`; adds no column and no write path.** Eligibility is deliberately copied from `ViewTrackingService::countEligibleDistinctViewersForTeaser()` / `MemberQuickHubService::resolveWhoViewedCountForUser()` — DISTINCT `viewer_profile_id`, admin viewers out, suspended viewer profiles out, blocked viewers out, self-views out — so the chart total can never contradict the who-viewed-me counter. Emits `date` (`Y-m-d`) only, never day names: the app is bilingual and formats its own labels. Zero-view days are emitted as `count: 0`, never omitted. One grouped query covers both the current and previous window. Do **not** add a second view-count aggregator; extend this one. |
| Suchak permissions / can-operate | `SuchakAccessService` | |
| Profile writes (all of them) | `App\Services\MutationService` | Owns the income key→column mapping. |
| Mobile onboarding step snapshots | `MobileProfileStepSnapshotService` | Rejects unknown keys and `*_option`; rejects `marriages`. |
| Photo URL resolution | `ProfilePhotoUrlService` | **Also owns "which photos does this profile have?"** — `visiblePhotoRelativePaths()` / `visiblePhotoUrls()` / `hasVisiblePhoto()` / `apiLegacyPhotoValue()`. Gallery rows (effectively approved, primary first) then the legacy `matrimony_profiles.profile_photo` column, and **a path with no bytes on disk is dropped**. The list card (`forListCard`), the hero (`forProfile`), the album (`ProfilePhotoAccessService`), `MatrimonyProfile::hasApprovedPublicPhoto()` and the raw `profile_photo` API field all read it, so no two screens can disagree about the same profile. This existed in four hand-copied versions; only two checked the file, which is exactly why a card showed no photo while the detail screen showed a broken one. Do not resolve a photo path anywhere else. **URL only** — who may *see* which photo (blur / tier / daily cap) stays with `ProfilePhotoAccessService`. |
| "May this profile upload N photos of this size right now?" | `App\Services\Image\ProfilePhotoUploadPolicy` (+ `ProfilePhotoUploadDenial`) | **The ONE gate for every member photo upload, on every surface.** Owns all four admin rules: `photo_max_upload_mb` (default 8, exposed as `maxUploadKb()` because Laravel's `max:` rule is in KB), `photo_max_per_profile` (default 5) vs `currentPhotoCount()`, `users.photo_uploads_suspended` (still `Schema::hasColumn` guarded) and the lifecycle lock (`LIFECYCLE_BLOCKING_STATES`). Two entry points, matching the order the checks must run in: `denyBeforeUpload($user, $profile)` (account/profile gate, before files are validated) and `denyForCount($profile, $incomingCount)` (1 for a single-photo surface, N for the gallery). It exists because only ONE of three upload paths enforced anything: `Api\MobileProfilePhotoApiController::store()` did all four, while the legacy `Api\MatrimonyProfileApiController::uploadPhoto()` (`POST /matrimony-profile/photo`, still called by published APKs) and `ProfileWizardController::buildPhotoSnapshot()` (website wizard) both hardcoded `max:2048` and checked nothing else — a suspended or lifecycle-locked member could upload freely, and the admin's MB/count dials were silently ignored on two of three surfaces. It **renders nothing**: each caller keeps its existing shape — the gallery its JSON plus `can_upload` / `max_photos` / `current_photo_count`, the legacy endpoint its bare `success` + `message`, the wizard a `ValidationException` on `profile_photo`. `MobileProfilePhotoApiController`'s own `currentPhotoCount()` / `profileLifecycleBlocksPhotoUpload()` now delegate here, so the gallery is behaviourally identical. Storage/processing stays `ImageProcessingService`; who may *see* a photo stays `ProfilePhotoAccessService`. Do not re-check a photo-upload rule in a controller. |
| Profile display payload | `MobileProfileDisplayPresenter` | |
| How strongly a locked profile/photo is blurred (the admin dial `profile_view_lock_blur_strength`) | `App\Services\Profile\ProfileViewLockBlurPolicy` | **The only reader of that setting, and the only place the 35–100 dial becomes a render value.** The clamp `max(35, min(100, …))` had been hand-copied into the admin form read, the admin form write and `MatrimonyProfileController`; all three delegate here now. Mobile got nothing at all, so the member app hardcoded its own blur and the admin's choice was silently ignored on Android — fixed by `photoBlurClass()`, emitted as `display.photo_album.blur_photo_class`. Deliberately reuses the teaser's `blur_photo_class` key + token grammar (`WhoViewedTeaserPresenter`) so the app parses it with `lib/core/locked_teaser.dart`; do not invent a third blur convention. **Strength only** — who may see which photo stays with `ProfilePhotoAccessService`, and this is NOT the nine-key teaser lock. Anchored at `78 → 40px` (the shipped default) so an app that ignores the key renders exactly what it renders today. |
| Ready-made Suchak service presets (Basic / Premium) | `App\Modules\Suchak\Support\SuchakDefaultPlans` | **SEED CONTENT since 2026-08-02, not a runtime authority.** It used to be code-defined and never seeded — which is exactly why a Suchak could not edit a ready-made plan: there was no row to edit, `applyOverrideFields()` accepted five fields, and the four fee columns `presetEntry()` READ were structurally always null. `seedRows()` is now the one seed shape, read ONCE by `SuchakCustomerPlanService::ensurePresetRows()` (lazily, on the first read of a Suchak's plans) and by migration `2026_08_02_300000` for accounts that already existed. After that the ROW is the plan and it is editable in full; the class is only a fallback for a legacy row whose columns were never filled. **Do not add a second reader of these constants at runtime** and do not re-introduce a code-vs-row merge. Still legitimately code-read at SEND time for a preset's `package_name`, description and stage/deliverable payload (`catalogPayload`) and for the "fold in Basic" toggle (`deliverablesForStage`) — those are what keep a customer's package scoped to one plan. |
| Suchak payment-received tracking feed | `App\Modules\Suchak\Services\SuchakPaymentRequestTrackingService` | **Read model over existing `suchak_payment_requests` — NO tracking table.** `GET /suchak/payment-requests` (search / filter=pending\|paid\|all / pagination / outstanding-totals summary). Customer name+mobile come from `customerContext.candidateProfile` (`full_name`, `primary_contact_number` accessor), plan name from `customerAgreement.package_name`. Always scoped to the authed `suchak_account_id`. Do not build a second list. |
| Suchak paid-mark reversal (request level) | `SuchakPaymentRequestService::reversePaidMark()` → `POST /suchak/payment-requests/{id}/reverse-paid` | Flips a PAID request back to OPENED/SENT; mandatory `reason` audited on the immutable `suchak_payment_request_events` trail + `SuchakActivityLog` (`payment_request_paid_reversed`). **Distinct from** the ledger-grade `SuchakCustomerPaymentCorrectionService::postReversal()` (rewrites the ledger on a recorded `SuchakCustomerPayment`). Mark-paid stays `SuchakCustomerPaymentService::recordManualPayment` via `POST .../mark-paid` (optional `note` → `collection_note`). |
| गुणमिलन / 36-guna Ashta-Koota scoring | `App\Services\Gunamilan\GunamilanService` | **The only scorer.** `calculate($viewer, $target)` for one pair; `kootaKeyFor($profile)` + `compare($brideKey, $groomKey)` for bulk (flatten each profile once, then every pair is pure array math, **0 queries**). Read `computable` before `total_points` — false means UNKNOWN, never "incompatible". Threshold `COMPATIBLE_THRESHOLD = 18.0`, **inclusive**. Do not re-implement any koota table elsewhere. |
| Gunamilan master-table snapshot | `App\Services\Gunamilan\GunamilanMasterData` | Container **singleton** + `Cache::rememberForever`. The one in-memory copy of rashis / nakshatras / yonis / gans / nadis / varnas / vashyas / rashi lords / mangal types. Owns the canonical yoni alias map and `isComputableKey()` (which is what stops the `other` sentinel rows being scored as real values). Call `forget()` after seeding or editing any horoscope master table. Never add a fresh `DB::table('master_…')` lookup to a per-pair code path. |
| One profile's flattened Gunamilan inputs | `App\Services\Gunamilan\GunamilanKootaKey` | Ten scalars: rashi position, nakshatra number, varna / vashya / lord / gan / nadi / yoni keys, mangal key, gender. Null means "not available", never "zero". Build once per profile per run. |
| मंगळ / Manglik comparison | `App\Services\Gunamilan\MangalCompatibility` | **Separate from the 36 gunas on purpose** — Ashta-Koota does not contain Mangal, so folding it into the total would produce a number no printed patrika could reconcile. Both-manglik and both-non-manglik are compatible; exactly one manglik is not; anything unknown is `not_computable` and must never be a rejection. `WEIGHT = 0.05` (low, per owner). |
| Horoscope dependency rules, autofill, validation warnings | `App\Services\HoroscopeRuleService` | nakshatra + charan → rashi; nakshatra → gan/nadi/yoni; plus the Tara / Bhakoot / Graha Maitri tables. Reads `GunamilanMasterData`, so its lookups are query-free. Key-based twins (`taraForNumbers`, `bhakootForPositions`, `grahaMaitriPointsForLords`) exist for the hot path — use those, not the id-based ones, inside a loop. |
| Member interest send / accept / reject / withdraw | `App\Services\Interest\InterestActionService` | **The ONE engine for every interest action, on every surface.** Web (`InterestController`), mobile (`Api\InterestApiController`) and the showcase auto-responder (`ShowcaseIncomingInterestResponderService`, via the shared `applyAcceptEffects()` / `applyRejectEffects()`) all call it; controllers only render the returned `InterestActionOutcome`. It exists because the web and API controllers each carried their own copy and silently drifted (2026-07-27): the API sent **no notification at all** — so an interest sent from the Flutter app never reached the receiver and the product looked like "nobody ever replies" — and it also skipped the block check, the paid reveal gate on accept, and the `profile_contact_visibility` + `contact_access_log` grant. Do not re-implement any interest guard in a controller, a job or a command. Refusals are `RuleResult`s from `ErrorFactory` (localized via `lang/{mr,en}/interest.php`), never bare strings. The reveal gate reuses `InterestSendLimitService::isIncomingInterestUnlocked()` — there is no second unlock predicate. Cover: `tests/Feature/Api/MobileInterestParityApiTest.php`. |
| Locked received-interest teaser card ("hide the identity, show the person") | `App\Services\Interest\ReceivedInterestTeaserBuilder` | **One builder for both surfaces** — the web inbox (`InterestController`) and the mobile list (`Api\InterestApiController`) — over the existing `WhoViewedTeaserPresenter` + `ReceivedInterestTeaserPolicy`. There is no second presenter and no second policy read. It exists because the web page had the teaser loop inline and mobile simply never got one: `GET /interests/received` returned a bare `{id, revealed:false}` for a locked row, so the paywall's whole pull — blurred photo, no name, real attributes — was invisible in the app (the Flutter side has read `interest['teaser']` via the shared `LockedTeaser` widget all along). Returns entries for **LOCKED rows only**; an unlocked row shows the real person, so callers may treat "has a teaser" as "is locked". Honours `rich_teaser_enabled` by returning an empty map (same as the web page always did) rather than a half-built card. `SENDER_PROFILE_EAGER_LOADS` is the eager-load set the presenter needs — load it for the whole page; loading only `gender` turns every locked row into a fistful of lazy queries. `displayShapeOnly` narrows to `WhoViewedTeaserPresenter::DISPLAY_KEYS`. |
| Which showcase profile to generate next (admin bulk) | `App\Services\Showcase\ShowcaseBucketPlanner` | **Photo-pool first, admin policy as the filter.** Reads `ShowcasePhotoPoolService::coverageMatrix()`, keeps only `eng/` buckets that still have an unused photo AND that `ShowcaseBulkCreateSettings::policy()` allows (religion / marital / age range / the run's gender), then hands `ShowcaseProfileFactory` a **narrowed copy of that same policy** (one religion, one marital status, the bucket ∩ policy age range) — so caste, names, diet and DOB stay internally consistent because they are still generated by the one existing engine, `ShowcaseProfileDefaultsService::fullAttributesForShowcaseProfile`. It exists because the old order was reversed (random attributes → derive folder → folder missing → `skip_profile`): with the shipped pool, 49 of 69 photos were unreachable and most creations silently vanished. Buckets the policy forbids are returned as `blocked` with a reason and shown on the bulk page, never silently dropped. `drawSlots()` issues one ticket per unused photo, so a bucket can never be over-drawn and the requested count is capped at what the settings can actually serve. Do not derive a showcase photo folder anywhere else — strict resolution stays `ShowcaseProfileDefaultsService::resolveShowcasePhotoForAttributes()`. |
| When a showcase→member view is allowed to land (naturalness / pacing) | `App\Services\Showcase\ShowcaseRandomViewService` — keys `KEY_MAX_PER_REAL_PER_RUN`, `KEY_MIN_GAP_HOURS_PER_REAL`, `KEY_JITTER_MAX_MINUTES` | **Three pacing rules, one engine, one write.** Added 2026-08-05 because the hourly batch wrote every view on the same second: production showed one member collecting 5 showcase views at `10:00:00`, and members read that as fake. `maxPerRealPerRun` stops one run from stacking views on a single member; `minGapSatisfiedForReal()` is the ONE definition of the per-member gap and is deliberately called twice — when the batch is planned and again inside `ProcessDelayedShowcaseRandomView` when a jittered view fires, because other views can land in between; `jitterMaxMinutes` moves *when* the row is written, never *who* writes it. The row itself is still created only by `ViewTrackingService::recordShowcaseRandomProfileView()`, so block / showcase-side / daily-cap guards are NOT restated in the job. **Trap: "don't show the same face again" is NOT one of these** — that is the pre-existing `showcase_random_view_revisit_mode` (+ `revisit_random_min/max_days`), governed by `pairAllowedByRevisitRule()`. Raise that setting; do not add a second same-face control. Related but separate: the week/month volume caps (`max_per_real_per_week`, `_per_month`) answer *how many*, these answer *how close together*. |
| Forcing Devanagari numerals back to Latin 0-9 | `App\Support\LatinDigits` | The one owner of the FROZEN "every numeral a member sees is Latin 0-9" rule, applied as a guard at the EXITS — every teaser string on its way into an API payload (`WhoViewedTeaserPresenter`) and every push title/body on its way to FCM (`PushTeaserCopyService::normalizeDigits()`). Hand-written copy was never the leak; locale-aware formatters are (Carbon relative time, `NumberFormatter`, admin-entered strings deciding for themselves how to render a number in `mr`). Do not add a second digit table. |
| The nine teaser keys every mobile surface emits | `App\Services\WhoViewed\WhoViewedTeaserPresenter::DISPLAY_KEYS` + `::displayPayload()` | The contract the Flutter member app reads (`lib/core/locked_teaser.dart` → `LockedTeaser.fromJson`), and exactly what the presenter returns. Both mobile surfaces narrow through the SAME static — the notification list (teaser decoded from stored JSON, could be anything) and the received-interest list (teaser straight from the presenter) — so neither can emit a wider or narrower shape than the client understands. Was a private const inside `Api\MobileNotificationApiController`; it is shared now precisely so a second copy never appears. |
| What language a stored notification teaser is READ in (and the actor it was built from) | `App\Services\WhoViewed\NotificationTeaserRenderer` + its `ACTOR_PROFILE_ID_KEY` (`teaser_actor_profile_id`) | **The one place a locked teaser is rebuilt at read time, shared by the mobile list (`Api\MobileNotificationApiController`) and the web list/detail (`NotificationController`).** A notification's `message` is stored in both languages (`message` + `message_mr`) and picked per reader; its `teaser` was stored in exactly ONE — whichever locale was active when the row was written. A profile-view notification is written inside the **viewer's** request, so it inherited the viewer's language and handed the recipient a card in the wrong one (confirmed on production, user 174). The teaser also carries a RELATIVE TIME, which froze at write time — every locked card read "Viewed just now" forever. Both are fixed by rendering from raw facts instead of storing presentation. `teaser_actor_profile_id` is the raw fact that makes this possible and is deliberately **NOT** `viewer_profile_id` / `sender_profile_id`: those mean "an actor this reader may see" and are scanned by both surfaces to build a tappable profile link, so reusing one would hand every locked row a working link to the person the paywall is hiding. **Old rows fall back, never blank:** a row without the key keeps its stored teaser (`storedTeaser()`, digits re-normalised), and a render that throws is logged and falls back too. Re-render is gated on the row already HAVING a stored teaser, so this is strictly an upgrade of a card the member already sees. `contextLabel()` does the same job for `teaser_context_label`, which had the identical single-language defect. Batched for the whole page (profiles + relations + repeat-view counts + policy): **measured 9 queries for 100 rows**, pinned by `tests/Feature/NotificationTeaserLocaleTest.php`. Reader locale comes from `SetApiLocale` (`app()->getLocale()`) — do not add a second locale mechanism. |
| Suchak match suggestion log (impression + decision) | `App\Modules\Suchak\Services\SuchakMatchSuggestionLogService` | **The only writer of `suchak_match_suggestions`.** `recordSuggestions()` (bulk upsert, idempotent per seeker+candidate+run_key, refreshes only `score`/`reasons_json` so a decision is never erased), `recordDecision()` / `recordDecisionForPair()` (chosen / rejected+reason code / ignored), `alreadySuggestedCandidateIds()` (exclusion set), `suggestedRecently($seeker, $days)` (still-too-soon set) and `cooledOffCandidateIds()` (re-surfacable). Decision + rejection-reason vocabularies are PHP consts on `SuchakMatchSuggestion` (varchar columns, no DB enums). It **records only** — it does not rank, filter or change any existing matching behaviour; the ranking engine stays `App\Services\Matching\MatchingService`. Do not write a second suggestion-history table. |

| "What is still worth filling in on this candidate?" (the section work-queue both Suchak screens show) | `App\Services\ProfileSectionReadinessService` | **A PRESENTER over `ProfileCompletionService`, not a third completeness reading — do not let it grow one.** Three readings would already have existed: the customer list's five `ONBOARDING_SECTIONS` ("was the onboarding run finished?", drives the list % and where the wizard resumes) and `ProfileCompletionService` (the member-side per-section `completed`/`incomplete`). This service adds no opinion about *done*: `ready` is gated on `ProfileCompletionService::getSectionStatus() === 'completed'`, so a section it calls ready is always one that service calls completed — never the reverse. All it adds is a FIELD CENSUS (`filled`/`total`, thresholds mirroring the member app's own readiness items) whose only job is to split "not done" into `missing` (nothing at all) vs `partial` (half filled) — the distinction a Suchak needs before phoning a customer. It can never promote a section to done on its own. Eleven rows in `config/field_catalog.php` order with `relatives`+`alliance` merged (one edit screen), labels resolved through `FieldCatalogService::getSectionLabel()` so the card, the wizard and the edit screen print the same word. Emitted by **both** `GET /suchak/customers/{representation}` (`data.customer.readiness`, the detail card) and `GET /suchak/nxt/{representation}/profile` (`data.completion.readiness`, the edit hub) from this one service — the hub used to print `calculateCompletionPercentage()` (five weighted buckets) beside an eleven-section count on one line, i.e. "100% complete · 4 section(s) need work" about one candidate. Residual by design: the customer LIST still shows the onboarding % (PO reversal 2026-07-26), which answers a different question. |
| "Does this profile's contact go through a Suchak?" | `App\Support\Suchak\SuchakContactRouting` | **The ONE routing predicate**, plus the routable-representation lookup, the Suchak display name and the Suchak's own (masked) number. It existed as five hand-copied private methods — `ProfileContactActionController`, `ContactRequestController`, `Api\ContactActionApiController`, `Api\ContactInboxApiController` and `MobileProfileDisplayPresenter` — which had already drifted on their `Schema::hasTable` guards, i.e. the same profile could be "routed" on one surface and not another. All five delegate here now. The only phone value this class can return is the **Suchak's** business number; the candidate's number is never read. |
| Suchak request lifecycle (create → view → reply → forward → candidate answer → SLA close) | `App\Modules\Suchak\Services\SuchakRequestPipelineService` | **The only writer of `suchak_profile_requests` / `suchak_pipelines` / `suchak_pipeline_events`.** Web (`PublicProfileRequestController`, `ProfileRequestReplyController`) and both apps (`Api\MemberSuchakRequestApiController`, `Api\Suchak\SuchakProfileRequestsApiController`) all call it, so an app request is indistinguishable from a web one (pinned by `SuchakRequestPipelineApiTest`). Consent is checked once, in `assertSuchakMayActOnRequest()`, reusing `hasValidConsent()` — no second consent rule. **First-answer-wins** lives in `recordCandidateDecision()`: candidate and Suchak both may answer, the race is decided under `lockForUpdate`, and the loser gets `already_answered` (attribution derived from the immutable pipeline event, **not** a new column). SLA close reuses `expirePipelineIfPastSla()` via the scoped `expireDuePipelines*()` sweeps — there is no cron and must not be a second timer. **SECOND ENTRANCE, 2026-08-05: `openPipelineForEngagement()`**, called by `SuchakCollaborationService::acceptRequest()` inside its transaction, so an accepted engagement gets the funnel entry the meeting engine hangs off (blueprint 5.1 — see "WHERE A PIPELINE CAME FROM" above). It is an entrance to THIS service, never a second creator: the row, its opening pipeline event and its activity row are written by one private `openPipeline()` that both entrances call, and `createRequest()`'s own behaviour is unchanged. What is deliberately NOT shared is the chat injection — it relays a MEMBER's opening message, and an engagement has neither. The engagement pipeline is opened `pending` with `lock_expires_at` NULL, so the SLA sweeps (which are scoped by an OPEN member request through `whereHas('request')`) can never reach it. |
| "This member's INTEREST is on a Suchak-managed profile" (heart → Suchak inbox, and back) | `App\Services\Interest\SuchakRoutedInterestService` | **The ONE bridge between `interests` and the Suchak request pipeline — it is NOT a second inbox, a second pipeline, a second predicate or a second thing the member sees.** It exists because the heart is the primary action on every card while the Suchak-request CTA is buried in the contact card, so in the common case the pipeline shipped 2026-07-27 was bypassed entirely: an interest to a routed profile created an `interests` row no Suchak could ever see (reproduced live — `interests` id 359). **No link column and no migration:** `interests` is unique on `(sender_profile_id, receiver_profile_id)` and a request already stores `(requesting_matrimony_profile_id, target_matrimony_profile_id)` — the same pair — so the mapping is derived and cannot drift. `request_reason = 'member_interest'` is an audit/backfill marker only; behaviour never keys on it. Routing reuses `SuchakContactRouting::isRouted()` + `SuchakRequestPipelineService::createRequest()`; consent is enforced by the code that already exists (no valid consent ⇒ never routed ⇒ nothing to answer, and `assertSuchakMayActOnRequest()` closes an already-created one). **Two-state sync is ONE transition**, `applyRequestDecisionToInterest()`, called from inside `recordCandidateDecision()`'s transaction under the same first-answer-wins lock: `candidate_interested` → `InterestActionService::applyAcceptEffects()`, `candidate_not_interested` → `applyRejectEffects()` — the accept side effects are reused, never re-implemented. A Suchak *reply* is deliberately not a decision (interest stays pending); an SLA expiry likewise leaves it pending, which is what lets a re-tapped heart open a FRESH pipeline request on the SAME single interest. **Quota:** routing charges the member nothing extra — the heart already paid `interest_send_limit`, so `FEATURE_CHAT_SEND_LIMIT` (what the CTA consumes) is deliberately skipped; the Suchak-side `assertLeadRequestAllowed()` is deliberately kept, since it is the Suchak's own paid capacity, not a member charge. Backfill: `php artisan suchak:backfill-routed-interests [--commit]` (dry run by default, idempotent, deletes nothing). Cover: `tests/Feature/Suchak/SuchakRoutedInterestTest.php`, whose last case is the regression guard that ordinary member-to-member interests are byte-identical. |
| Suchak request payload shape (member + Suchak + contact card) | `App\Modules\Suchak\Services\SuchakRequestPresenter` | One shape for all three surfaces: `memberRequestPayload()`, `suchakRequestPayload()`, `suchakBlock()`, `contactStateFor()` and the shared `decisionResponse()` both apps read. Owns the four additive contact-card states (`suchak_request_available` / `_pending` / `_answered` / `_closed`) and status labels from `lang/{mr,en}/profile.php` (`suchak_request_status_*`, which already covered all nine states). Never emits a candidate contact number. |

| Chat reply gate (consecutive-messages-without-reply limit + cooling period), for EVERY role | `App\Services\Chat\ChatPolicyService` + `CommunicationPolicyService::replyGateLimitsFor()` | **One gate, one cooldown table, one counter — only the two thresholds are role-scoped.** A member gets `communication_max_consecutive_messages_without_reply` / `communication_reply_gate_cooling_hours`; a Suchak gets the `communication_suchak_*` pair (defaults 4 / 24h, admin-editable on the same Communication Policy screen). Do **not** add a Suchak-only policy class, a second gate or a bypass. The role cannot be inferred from the sender: `SuchakRequestPipelineService::replyThroughExistingChat()` relays a Suchak's words into the member↔candidate conversation under the **candidate's** `sender_profile_id`, so the acting role is passed explicitly (`CommunicationPolicyService::ACTOR_MEMBER` / `ACTOR_SUCHAK`) through `ChatMessageService::sendTextMessage()`; every other caller keeps the member default. Because the relay wears the candidate's profile, the candidate's own messages and their Suchak's relayed ones deliberately **share one trailing counter and one `message_policy_cooldowns` row** — the gate protects the receiver, who sees a single profile writing, and separate counters would let the pair fire 2 + 4 unanswered messages at them. Cover: `tests/Feature/Chat/FreeChatReplyGateTest.php` (member, unchanged) + `tests/Feature/Suchak/SuchakReplyGateThresholdTest.php`. |

| Cross-Suchak engagement lifecycle **and** its marketplace stage ladder | `App\Modules\Suchak\Services\SuchakCollaborationService` | Already owned create / accept / reject / expire / commission terms / collector lock on `suchak_collaboration_requests` + `suchak_commission_agreements`; it is now also the **only writer of `suchak_collaboration_stage_events`** (`claimStage()`, `claimCustomerStage()`, `recordCustomerStage()`, `confirmStage()`, `linkCustomerAgreement()` — all five funnel into the one private `writeStageEvent()`). **Their doors** (added 2026-08-02 — until then all four were reachable only from tests, so `customer_owner_side` could only ever hold its default and `marketplace_stage` / `customer_agreement_id` could only ever be NULL): `SuchakCollaborationStagesApiController` on the Suchak API for the three Suchak-acted writers, and `MemberSuchakStageApiController` on the MEMBER API for `confirmStage()`. The confirm half is on the member API deliberately and must stay there — the service refuses a *participating* Suchak but accepts any other user, so a Suchak-authenticated confirm route would let an unrelated verified Suchak settle a stranger's marriage claim and release a success-fee tranche. **The FAMILY's own confirm door landed 2026-08-04** — `CustomerStageDoorController::confirm()` on the same tokenised portal link, passing the link itself to `confirmStage()` (which now takes `User|SuchakCustomerPortalLink`). It is the SAME write, not a second one: until it existed, `§2`'s login-less family — the party D26 names as the confirmer — could not settle any of the three rungs that release the success fee, and a Suchak reaching that door is refused there as flatly as `assertClaimChannel()` refuses him on the claim rungs. Do not add a second stage recorder: `SuchakActivityLog` is a single-actor audit trail with no confirmation slot, `suchak_pipeline_events` belongs to the member→Suchak request pipeline, and `suchak_visit_confirmations` is the per-MEETING claim/confirm engine (blueprint 5.1) that this ladder points at rather than replaces. **The CUSTOMER's own door landed 2026-08-03**: `recordCustomerStage()` behind the public, tokenised `CustomerStageDoorController` (`/suchak/customer-portal/{token}/stages`), which is how `viewed` / `interested` / `meeting_confirmed` became recordable at all — they belong to the family, every Suchak is still refused them, and both member-API doors need a login the family (§2) usually does not have. It records the ladder rung only and deliberately does NOT touch `suchak_visit_confirmations`: a tokenised second confirmation path into the meeting engine would be a second confirmation engine, which §12 forbids. |
| Meetings between a pair: schedule, complete, confirm, dispute, qualify the visit payout | `App\Modules\Suchak\Services\SuchakVisitConfirmationService` | The ONLY writer of `suchak_visit_confirmations` + `suchak_visit_confirmation_events`. Seven public methods; until 2026-08-01 only `scheduleVisit()` and `markSuchakCompleted()` had a route, so a production row could never leave `scheduled`/`completed`. The other four are now exposed on the surface each actor belongs to, NOT all on the Suchak API: `confirmByUser` + `disputeVisit` on the MEMBER api (`POST /api/v1/suchak-meetings/{visit}/confirm` and `/dispute`) because `assertRequestingUserCanConfirm()` accepts exactly the owner of `requesting_matrimony_profile_id`; `confirmByAdmin`, `disputeVisit` and `qualifyPayoutForVisit` on the admin web surface (`admin/suchak/visits/*`, `VisitConfirmationController`) because the service opens both admin methods with `assertAdmin()`. **The arranging Suchak cannot dispute their own claim** — he is the claimant, and `cancelVisit()` already refuses him once the meeting is marked complete for the same reason; D26 “either Suchak may raise the claim” is about the STAGE LADDER, not about a visit dispute. **The HELPING Suchak can** (added 2026-08-03): `visitDisputeActorType()` now admits admin → the account named in `helper_suchak_account_id` → the customer-side member, in that order, and `POST /api/v1/suchak/meetings/{visit}/dispute` is his door. §7.2's stop-loss is built on UNANSWERED HELPER CLAIMS and the helper previously had no way to raise one. `helper_suchak_account_id` is the only link consulted — a visit row carries no `collaboration_request_id`, and `customer_owner_side` names a SIDE, not an account. The hold that dispute opens still lands on `suchak_account_id` (the arranging Suchak), which is exactly §7.2's leverage. Do not add a second meeting engine and do not re-implement its authorisation in a controller. |
| What a CLOSED dispute means for a disputed meeting's money | `suchak_visit_confirmations.refund_review_status` (`not_requested` → `pending_review` → one of `upheld` / `dismissed` / `closed_no_finding`) | `SuchakVisitConfirmationService::disputeVisit()` opens it; `SuchakVisitConfirmationService::settleDisputedVisits()` is the ONLY writer of the three closing values, called by `SuchakSafetyService::transitionDispute()` when a dispute reaches a closing status | Admin: `POST admin/suchak/safety/disputes/{dispute}/close` (the settlement is a side effect of closing the case, not a separate screen) | Until 2026-08-03 the vocabulary had only the first two values, the transition was one-way and **nothing in app/ wrote it back**, so `pending_review` was terminal — combined with guards that tested `dispute_id !== null` (a trail marker that is never cleared), a dispute resolved IN THE SUCHAK'S FAVOUR left the meeting unpayable forever. The three closing values are three DIFFERENT money answers and must not be collapsed: `rejected` → `dismissed` (**the freeze lifts, the money does not move** — the hold is released and every ordinary door reopens, but the meeting goes back to awaiting the family's answer); `resolved` → `upheld` (fee refused permanently, visit moves to `cancelled` so a settled case stops blocking the pair); `closed` → `closed_no_finding` (no conclusion — the row falls back to its own confirmation columns, M4 intact). **`dismissed` used to also substitute for the customer's confirmation, and that broke M4** (fixed 2026-08-03): `assertEligibleForPayout()` accepted it INSTEAD of `confirmationPolicySatisfied()` and `refreshVisitStatus()` moved the row to `confirmed`, so one admin form post to the close route made a fee fall due on a meeting nobody had ever confirmed, with the customer nowhere in the transaction. An admin deciding a dispute is not the customer confirming and no mapping between them is legitimate. The concern that motivated the substitute — that disputing would otherwise be a free permanent veto on the fee — is answered by the un-freezing instead: `confirmByUser()` accepts a row whose column still reads `disputed`, so the family that lost its contest can confirm, and their own act is what moves the money. If they never answer, §7.2 clause 4 ends the claim at 90 days with nothing owed, which the blueprint states outright. Mapping lives once in `SuchakVisitConfirmation::DISPUTE_CLOSE_REFUND_OUTCOME`. `dispute_id` / `payout_hold_id` are NEVER cleared: the trail has to outlive the case. Ask `hasOpenDispute()` / `isFeeRefusedByReview()` / `isComplaintDismissedByReview()` — never `dispute_id !== null`. The last of those was called `isFeeAllowedByReview()`, and the name was the bug in one word: it allowed no fee, and two call sites believed it did. |
| Lifting a payout hold | `suchak_payout_holds.hold_status` + `released_by_user_id` + `released_at` + `release_reason` | `SuchakSafetyService::releasePayoutHold()` → `applyHoldRelease()` — the ONLY writer of the four columns, shared by the manual door and by the cascade a dispute close performs | Admin: `POST admin/suchak/safety/payout-holds/{payoutHold}/release` (route name inside the `admin.suchak.safety.*` pattern the nav catalog already claims, so `EnsureAdminSectionAccess` gates it) | The statuses and the three release columns shipped 2026-06-10 with **no writer anywhere in app/**, so a hold — which blocks EVERY later platform payout for that Suchak/context — was permanent once created. Admin only: never the Suchak whose money it is, never the customer. Requires an active hold plus a written reason, and writes an `AdminAuditLog` + a `SuchakActivityLog` (`payout_hold_released`) bound to it — the logger refuses an admin row without an audit id, which is what makes it an audited act. `released` ≠ `cancelled`: see `SuchakPayoutHold::DISPUTE_CLOSE_HOLD_OUTCOME` (`rejected` → `released`, `resolved`/`closed` → `cancelled`). A hold is never re-armed in place; open a new one. |
| Publishing a customer to the marketplace, answering a challenge, and what a browsing Suchak sees | `App\Modules\Suchak\Services\SuchakMarketplaceChallengeService` | The ONLY writer of `suchak_marketplace_challenges` (`publish`, `withdraw`, `expireDue`), the only reader that shapes a listing (`browse`, `openListing`, `published`, `listingPayload`), and the only entrance to ACCEPT-BY-PROPOSING (`proposeCandidate`, `proposalsFor`, added 2026-08-03). **Its door is `SuchakMarketplaceChallengeApiController`** on the Suchak API — seven routes under `/api/v1/suchak/marketplace/challenges`, one per capability, each added in the same commit as the capability. **There is deliberately no bare-accept endpoint** (D7): `proposeCandidate()` takes the helper's own `representation_id` and the act of naming a candidate IS the acceptance. It creates no object of its own — it guards, then delegates to `SuchakCollaborationService::createRequest()` with the challenge attached (the engagement is the existing pair, reversed, blueprint 6.1) and records `profile_suggested` through `claimStage()`, the one stage writer. The publisher answers on the EXISTING `POST /api/v1/suchak/collaborations/{collaboration}/accept|reject`, which already gate on the target actor — he is the target in this direction — so no second accept verb exists. Three things it deliberately does NOT own, each already having exactly one owner: the candidate's cross-Suchak presentation (`SuchakCandidateMaskingService::maskedSummary`, called verbatim — the moment this service starts deciding what another Suchak may see, D19a is enforced by whichever of two rules the caller happened to reach); the `published_to_marketplace` ladder row (`SuchakCollaborationService::claimCustomerStage()` is called, not reimplemented — and the already-recorded case is decided by RE-READING, never by matching the exception's message, because a message is not an interface); and the success fee (`suchak_service_packages.post_marriage_fee_amount`, read against, never copied). **Marketplace participation is gated on `SuchakAccount::isVerified()`, deliberately STRICTER than `SuchakAccessService::canOperate()`** — `canOperate()` admits a PENDING account when the policy allows work before admin approval, which is right for a Suchak building his own book and wrong here, because A10's attack is one person running two accounts and colluding and an unverified account is exactly the cheap second one. Being stricter, it satisfies `claimCustomerStage()`'s own `canOperate()` check by construction. |
| Cross-Suchak money: raising a declared share into a debt, settling it, and the A7 ratio | `App\Modules\Suchak\Services\SuchakCrossSuchakObligationService` | The ONLY writer of `suchak_cross_suchak_obligations` (`raise`, `settle`) and the only place M3’s two halves, A7’s realized-vs-declared ratio and §7.3’s exposure figure are computed. **Its door is `SuchakCrossSuchakObligationApiController`** — six routes, one per capability, all added in the same commit. It creates NOTHING of its own beyond its own rows: the marriage is `SuchakMarriageOutcomeService`’s, the tranche ledger is `SuchakSuccessFeeTrancheService`’s (`settled_at` / `customer_payment_id` are READ here, never written — a second writer would be a second answer to "has the customer paid"), the declared share and its rupee value are `SuchakMarketplaceChallenge`’s, the participant/acceptance gate is `SuchakCollaborationService::assertAcceptedParticipant()`, the `share_settled` rung goes through `claimStage()` (the one stage writer), and every rupee string goes through `App\Support\MoneyFormat`. It deliberately touches **no payout file** — see the §7.3 row in section 1. |
| The §7.2 claim clock: seven silent days, the stop-loss, the ninety-day lapse | `App\Modules\Suchak\Services\SuchakClaimSilenceService` | Decides WHICH rows; writes none itself. The visit row is written by `SuchakVisitConfirmationService::openSilenceDispute()` (the one writer of `suchak_visit_confirmations`) and a lapsed case is closed through `SuchakSafetyService::closeDispute()` (the one audited path, which already cascades `settleDisputedVisits()` + `releaseHoldsForClosedDispute()`). No new dispute vocabulary, no second hold engine. **Three doors, and three independent guarantees, because production may not run `schedule:run` at all and the notifications/governance queues have had no worker since 2026-06-17:** (a) the daily sweep is the NINTH sub-job of `SuchakScheduledJobsConsolidationService` — synchronous, no `ShouldQueue` on the path — placed LAST because `runTrackedJob()` re-throws and aborts every sub-job after a failure, and it catches per row so it cannot abort even itself; (b) the lazy sweep runs on the read path scoped to one account (`sweepForAccount()` / `unansweredClaimSummaryAfterSweep()`), wired to the stop-loss gate itself so the counter is swept immediately before it is judged; (c) the lapse's money conclusion is ARITHMETIC (`isClaimLapsed()`), enforced at `assertEligibleForPayout()`, so "never due" needs no sweep to have run. The 500-row cap is respected, ordered OLDEST FIRST and reported as `due_total` / `deferred_backlog` in `metrics_json` — a money job may be slow but must never silently skip row 501. **The stop-loss door is `SuchakMarketplaceChallengeService::proposeCandidate()`** — D7 makes proposing the only way to accept a challenge, so it is the only door the rule needs. Scoped to the ORIGINATING Suchak across ALL helpers, not per pair: per-pair counting would let him stonewall one helper and go on recruiting the next three. |
| A Suchak's BEHAVIOURAL record — what his history says to another Suchak (§11 phase 5, D12, D13) | `App\Modules\Suchak\Services\SuchakReputationService` | A pure READER. Writes nothing, stores nothing, and adds no table — §5.5: *"add a reader over existing event tables, not a new event log"*. **It is NOT `SuchakQualityControlService`** and must never be merged with it: §12 forbids a second 0-100 score, and that one is an ADMIN RESTRICTION risk score — a genuinely different fact that keeps a different name. **Two blocks are bound verbatim, never recomputed:** `declared_share` is `SuchakCrossSuchakObligationService::declarerRatio()` (A7) and `unanswered_claims` is `SuchakClaimSilenceService::unansweredClaimSummaryAfterSweep()` (§7.2 clause 2, swept for the reason that method's own docblock gives — a card reading "0 unanswered" while clause 3's gate refuses the helper two seconds later is worse than no card). Everything else is counted on the read from columns their own engines own: `suchak_visit_confirmations` (arranged and as helper), `suchak_collaboration_stage_events` on `CONFIRMABLE_STAGES` (D26 claim→confirm), `suchak_collaboration_requests`, `suchak_marriage_outcomes` (through `SCOPE_LIVE`, so a voided claim stops counting for free) and `suchak_marketplace_challenges`. **`MIN_RATE_DENOMINATOR = 5` is the one place a proportion is decided** — see the §1 row on it. **Its door is `SuchakReputationApiController`**: own card ungated, another Suchak's gated on the VERIFICATION badge (D18/A10), the same gate `declarerRatio()`'s route already applies. |
| A CUSTOMER's own trail — what was arranged for this family and what happened (D20) | `App\Modules\Suchak\Services\SuchakCustomerHistoryService` | A pure READER, keyed on `suchak_customer_contexts.id` — the same key `SuchakTwelveMonthClauseService` uses for the same family. Writes nothing and adds no table. Meetings come from `suchak_visit_confirmations.customer_context_id`; the cancelling ACTOR from `suchak_visit_confirmation_events` (§5.1 B4's "cheapest home"); suggested/viewed/interested from `suchak_collaboration_stage_events`; the marriage from `suchak_marriage_outcomes`. **Engagements are resolved by the two RECORDED links** — a commission agreement naming one of this context's agreement revisions, or a challenge published on one — never by `customer_owner_side`, which defaults to `target` and is written only by `linkCustomerAgreement()`. **NO MONEY IS PUBLISHED** (D17, and §15's record of that instinct being rejected twice): a screen built from this payload cannot become the regret ledger, because the payload has no rupee in it. **Its door is `SuchakCustomerHistoryApiController`, OWNER-SCOPED ONLY** — 404 for anybody else, never 403. §9 also admits this history to a helping Suchak *before accepting* and to other verified Suchaks; that wider surface is deliberately NOT built, and the gap is recorded rather than implied. |
| Every proposal made for ONE of my candidates, across every challenge I published for him (§11 phase 5, §16) | `App\Modules\Suchak\Services\SuchakCandidateProposalInboxService` | A pure READER, and the inverse axis of `SuchakMarketplaceChallengeService::proposalsFor()` — that one is still the right door for accepting or rejecting ONE challenge's answers; this one is for comparing answers given under different challenges at different shares. **It owns four things it must never re-own:** the proposal row (`proposalPayload()`, verbatim — which is also where the masking happens), the badge (`assertMarketplaceViewer()`, made public rather than copied), the candidate filters (`SuchakCrossSearchService::applyCrossSuchakProfileFilters()`, the one filter owner in cross-Suchak mode) and the score (`SuchakMatchFitService::fit()` with the PROPOSED candidate's representation as the masked side, so trap 14's cap applies). It adds exactly three keys on top of the shared row — `responded_at`, `candidate_consent_valid` and `fit`. **Its door is `SuchakCandidateProposalInboxApiController`.** |
| What the marketplace looks like to a Suchak deciding whether to publish, and at what share (§11 phase 5, §9) | `App\Modules\Suchak\Services\SuchakMarketEconomicsService` | A pure READER over three existing tables; adds no table, no column and no cache. **It is NOT `AdminDashboardMetricsService`** (§13: platform-wide only, never scoped, and a different audience and gate) and it is NOT the reputation read — that one describes ONE named Suchak to another, this one describes the population and names nobody. Every figure passes through one `block()` helper that applies the twin population thresholds; there is no path around it. Bound, never recomputed: `assertMarketplaceViewer()` and `audiencesAdmitting()` (so `open_now` counts exactly what `browse()` lists), `expireDue()` (so "open" means the same on both screens), `SuchakMarketplaceChallenge::declaredShareTotal()` / `declaredShareCurrency()` (so the median and the listing quote one arithmetic), and the `SCOPE_LIVE` global scope on `suchak_marriage_outcomes` (so a voided claim stops counting for free). **Its door is `SuchakMarketEconomicsApiController`.** |
| A rupee or a percentage a HUMAN reads | `App\Support\MoneyFormat` (money) and `App\Support\PercentDisplay` (percent) | Two frozen workspace rules meet in both: Latin digits 0-9 in either language, satisfied by construction because nothing in either class is locale-aware, and Indian comma grouping, which `number_format()` alone gets wrong above a lakh. Null in / null out in both: "not quoted" and "not computable" are real answers the caller words for itself, and printing ₹0 or 0% states the opposite. **`PercentDisplay` was added 2026-08-05 because `readablePercent()` had already been written privately FOUR times and the copies had drifted — two rounding to one decimal and two to two, so one 12.25% read as "12.3" on one Suchak screen and "12.25" on the next.** `SuchakReputationService` and `SuchakMarketEconomicsService` bind to it; `SuchakCrossSuchakObligationService`, `SuchakSuccessFeeTrancheService` and the inline copy in `SuchakMarketplaceChallengeService::declaredSharePayload()` are the remaining migration debt. **Do not add a fifth.** |

Full lists: `app/Support/*.php`, `app/Modules/Suchak/Services/*.php`.

---

## 3. Duplicate traps that already bit us

Each of these was a real defect, not a hypothetical:

1. **Two gender inputs** on one wizard step, unlinked, writing the same column by
   two paths (2026-07-22). → One visible input; pass the value on.
2. **Invented "number of children"** where the reference flow uses repeatable
   rows (2026-07-22). → Mirror the reference flow exactly; do not design a
   "simpler" alternative.
3. **Gate removal deleted the field** — mother tongue stopped being collected
   when its gating behaviour was removed (2026-07-22).
4. **Contact fields accepted where no column exists** — silently dropped, later
   re-prohibited so clients get an honest 422 (2026-07-21 → 22).
5. **`user.email` used as the identity line** for OTP-registered Suchaks, so the
   admin list showed bare names (2026-07-23).
6. **Suspected-but-innocent:** `income_amount` vs `annual_income` looked like a
   duplicate and is not — one is derived from the other. *Check before
   "fixing".*
7. **A filter shipped for a value the surface hides — twice, in one slice
   (2026-08-03).** D7a added location and income filters to the cross-Suchak
   masked search. `whereResidenceUnderAncestor()` accepts an ancestor at any
   depth, so a **village id** sent as `district_id` narrowed the page to that
   village and read back the village D19a hides; and because no masked card
   prints an income at all, a **binary search over `income_min`** recovered an
   exact salary in 23 requests. Neither is a leak in the response body — both are
   leaks in the **result count**. Rule: adding a filter to a masked surface is a
   privacy decision, not a search decision. Ask what the card prints; if it does
   not print the value, the filter belongs on the own book only. Both attacks are
   now `SuchakCrossSearchOracleTest`.
8. **`suchak_plans` vs `suchak_customer_plans` — same word "plan", different
   meaning.** `suchak_plans` is the PLATFORM subscription catalog a Suchak *buys*
   (route `GET /suchak/plans`, `SuchakBillingApiController`, consumed live by
   Suchak-apk). `suchak_customer_plans` is the per-Suchak reusable presets a
   Suchak *sells* to their customers (route `/suchak/customer-plans`,
   `SuchakCustomerPlanService`). Never mount one on the other's route (the new
   resource is deliberately at `/customer-plans`, not `/plans`, to avoid
   shadowing the platform endpoint), and never reuse one model for the other.
9. **Never re-add `source_template_id` / `template_*` FKs or `*_json` stage
   columns to `suchak_service_package*`.** The reusable-plan "template" now lives
   in the separate, mutable `suchak_customer_plans` table and is materialized at
   send with NO back-FK. `SuchakServicePackage` stays immutable / admin-approved /
   per-customer. `SuchakPackageRateCardFoundationTest` guards those send-time
   tables — do not "link" the two models.
10. **`profile_matches` vs `suchak_match_suggestions` — both hold (profile,
   matched profile, score, reasons), and they are NOT interchangeable.**
   `profile_matches` is a *cache of the current answer*:
   `MatchingService::replacePersistedMatches()` wipes every row for a profile and
   re-inserts, so asking it "what did we already show this seeker last month" is
   always wrong — the evidence was deleted. `suchak_match_suggestions` is the
   *append-only history*: what was shown, when, with the frozen score/reason
   snapshot, plus the Suchak's decision. Never read history out of
   `profile_matches`, never bulk-delete `suchak_match_suggestions`, and never add
   decision columns to `profile_matches` (2026-07-26).
11. **The same 14 yonis existed twice, in two languages, both active**
    (2026-07-26). `master_yonis` held the Sanskrit set (`ashwa`, `gaja`, …) that
    `master_nakshatra_attributes` autofills from, **plus** an English set
    (`horse`, `elephant`, …) added later by a seeder. Both appeared in the
    dropdown, and `GunamilanService::YONI_ENEMY_PAIRS` was written in English —
    so the enemy-pair rule never fired on an autofilled profile, and the same
    animal under two spellings failed the `===` test. **4 of the 36 gunas were
    wrong on every autofilled profile.** Fixed by making Sanskrit canonical,
    remapping the FKs and deactivating the duplicates. A near-miss of the same
    shape: `keet` was missing from the Vashya pair table, so every Vrishchika
    pairing hit an undocumented 0.5 fallback.
12. **"No data" scored as "incompatible"** (2026-07-26). `calculate()` set
    `available` from the mere EXISTENCE of a `profile_horoscope_data` row, so a
    row of NULLs returned `available: true, total_points: 0/36` and a naive
    `>= 18` check called it INCOMPATIBLE — which would have excluded every
    member who never opened the horoscope step. A `computable` flag and a
    nullable `is_compatible` now separate "0 points" from "unknown". The same
    class of bug: the `other` rows on `master_gans` / `master_nadis` /
    `master_yonis` / `master_rashis` / `master_nakshatras` mean "did not know",
    so comparing two `other` nadis as equal invented a Nadi dosha out of
    nothing. `GunamilanMasterData::isComputableKey()` is the one place that
    decides a sentinel is absence, not a value.
13. **A threshold that drifted by one** (2026-07-26).
    `MobileProfileDisplayPresenter` tested `<= 18.0`, quietly making the
    owner's "18 of 36 is compatible" rule mean 19+. Thresholds live on
    `GunamilanService::COMPATIBLE_THRESHOLD`; do not re-type the literal.
14. **The score confirmed what the card hid — trap 7 a third time, through a
    different channel (2026-08-03).** Same response, same masked village: the
    fit explanation shipped beside the masked card resolved FINER than the card.
    `MatchingService::scoreLocationPart()` paid an exact `location_id` match the
    full location weight with the reason *"same city"* and a taluka-only match
    90% with *"same taluka"*, so one own candidate per village under the shown
    taluka, passed as `requesting_representation_id`, read D19a's hidden village
    out of `match_field_points['location']` — one request per guess, no counting.
    The same component leaked a second time through arithmetic:
    `reason_nearby_taluka` printed the kilometres, and
    `MatrimonyProfile::leafGeoBundle()` takes lat/lng from the LEAF, so three
    probes in a neighbouring district trilaterated the village position.
    **Rule: a masked surface's SCORE is part of the disclosure.** Whatever the
    filters may narrow by, the explanation may resolve by — no finer. Fixed by
    capping precision, never by deleting the signal (D19a: a matchmaker who
    cannot see enough cannot propose a match). The cap is a parameter on
    `computeMatchBreakdown()`; the DECISION to use it is taken only in
    `SuchakMatchFitService`, from `SuchakCandidateMaskingService::revealsVillage()`
    — the one reader of `shares_village`. The member path never reaches that
    class and keeps the exact tier (D19b). `SuchakCrossSearchOracleTest`.
15. **Two memos for one fact, and the second one keyed by the wrong thing
    (2026-08-03).** The cap in trap 14 held; the oracle failed anyway, but only
    when another test class had run first — four probes identical except for
    their village came back 53/53/55/55, and the difference was in
    `preferences`, not `location`. Cause: `ProfilePreferenceMatchService::$residenceGeoCache`
    and `MatchingService::$residenceGeoCache` each memoised "district / state /
    country / taluka of this profile" **keyed by PROFILE ID**, on top of
    `MatrimonyProfile::geoAddressIdsForLeaf()`, which already memoises the same
    walk keyed by `addresses.id`. The address key cannot go stale (an address's
    ancestry is master data); the profile key can, because a profile moves — and
    both memos were only ever cleared in `MatchingService::findMatchesForTab()`,
    the member-feed entry point, which the Suchak fit path never takes. So in
    any long-lived PHP process (queue worker, Octane; and in the suite, re-used
    profile ids across test classes) a candidate kept scoring against their OLD
    taluka. **Rule: memoise a fact under the key that fact actually depends on,
    and if a canonical memo for it already exists, delegate — a second memo is a
    second home.** Both were deleted in favour of the leaf-keyed one, and
    `$residenceDisplayCache` was re-keyed from profile id to `addresses.id` for
    the same reason. The oracle now warms the process deliberately (two probes
    scored once from another district, then moved back) so it fails under EVERY
    ordering rather than one. **Note what this was and was not:** the cached
    tuple stops at the taluka, so it could never name a village — what it
    produced was a fit explanation that depended on process history instead of
    on the pair, which is enough to make two masked candidates distinguishable.

15. **Two fail-open gates, both of which let a MARRIAGE claim stand on nothing
    (2026-08-04, blueprint §6.2 phase 4).** Neither was a missing rule; both were
    a rule placed where it did not run.
    (a) `SuchakCollaborationService::assertStageClaimant()` returned EARLY for
    `CLAIMANT_EITHER_SUCHAK` — **above** the "this engagement names no customer
    agreement revision" refusal. D26's *"either Suchak may raise the claim"* is
    about WHICH of the two may speak; it was being read as "and about nothing
    else". Every rung with a NARROWER claimant already passed through that
    refusal, so `meeting_scheduled` and the three TERMINAL rungs —
    `marriage_settled`, `engagement`, `marriage`, the ones that release the
    largest tranches in the system — were the **only** rungs exempt from it. A
    marriage could therefore be claimed on an engagement with no agreement
    revision, no frozen success fee, no tranche shares and `customer_owner_side`
    still on its column default: a §6.2 attribution with nothing to attribute.
    The refusal is now unconditional for every Suchak-claimable rung, which is
    what makes `suchak_marriage_outcomes.customer_agreement_id` NOT NULL
    possible — **the two are one change**.
    (b) `confirmationActorType()` mapped admin to `ACTOR_ADMIN`, threw for a
    participating Suchak's user, and let **every other authenticated user
    through** as `ACTOR_USER`. The check that actually protected it — "you must
    be one of the two candidates" — lived in `MemberSuchakStageApiController`.
    So the SERVICE's guarantee was "not a participating Suchak", and any second
    door onto `confirmStage()` would have let a stranger with a login settle a
    marriage claim worth tens of thousands. **The rule was MOVED into the
    service** (§12 forbids a second confirmation engine, and a rule enforced in a
    controller is a rule the next controller does not have); the controller's
    check stays as the **privacy** answer — 404, so a stranger never learns the
    engagement exists — rather than as the security boundary. Order matters and
    is unchanged: a participating Suchak is refused **first**, so a Suchak who is
    also a candidate on his own engagement cannot confirm his own claim.
    `SuchakMarriageOutcomeTest` pins both.

16. **A table with the right VOCABULARY and the wrong SHAPE — the near-duplicate
    that cost nothing only because it was checked first (2026-08-04, M2/M3).**
    `suchak_ledger_entries` reads like the cross-Suchak obligation: it already
    has `TYPE_SUCCESS_FEE_EXPECTED`, statuses `expected/due/paid/waived`, a
    `due_date`, a `paid_at` and even a `collaboration_request_id`. Every word
    the blueprint's money rules use is already in it. It still cannot express
    "Suchak A owes Suchak B", and no amount of vocabulary fixes that: it has ONE
    `suchak_account_id` and a **NOT NULL `matrimony_profile_id`**, so it is one
    Suchak's receivable against a PERSON. Adding `payer_suchak_account_id` would
    have (a) forced the married candidate into a column that then reads as "this
    family owes it", (b) rested a debtor/creditor pair on an `entry_type` a human
    picks from a `Rule::in` dropdown on two Blade forms — `CrmLedgerController`
    and `CollaborationController`, written by no engine — and (c) silently
    changed the meaning of every existing reader (the CRM ledger screen, both
    dashboards, `SuchakIncomeAnalyticsService`, `SuchakWorklistSourceQueries`),
    none of which filters on a column that did not exist when it was written.
    **Rule: matching vocabulary is not matching shape. Before reusing a table,
    read its NOT NULL columns and ask who writes its type — a row whose
    existence depends on somebody choosing the right string from a dropdown is a
    note, not an obligation.** The same check cleared three other candidates in
    one line each: `suchak_platform_payouts` (platform to ONE Suchak, no payer),
    `suchak_customer_payments` (customer to ONE Suchak, and no way to say WHICH
    fee) and `suchak_commission_agreements` (acceptance-only — pending/accepted/
    rejected/cancelled, with no due, paid or settled anywhere).
    `SuchakCrossSuchakObligationTest::test_no_existing_money_table_can_name_a_payer`
    pins all four as assertions, so the next "just add a column" trips over it.

---

## 4. The pre-flight check

Cheap (seconds). Never skip, even in fast mode:

```bash
grep -rn "the_field_name" app/ database/migrations/ resources/views/
```

Then state the result in one line before writing:
*"`X` already exists in `Y` — binding to it"* **or** *"`X` does not exist anywhere — adding it, map updated."*

Test suites are expensive and are skipped by default (see the Speed contract in
the workspace `CLAUDE.md`). **This grep is the cheap check and is not optional** —
it is what actually prevents duplicates.
